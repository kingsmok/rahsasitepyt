# 🚀 راهنمای گام‌به‌گام نصب آکادمی آنلاین روی هاست سی‌پنل (cPanel)

این پروژه به‌طور کامل برای اجرا روی هاست‌های اشتراکی سی‌پنل (مجهز به **CloudLinux** و **Phusion Passenger**) بهینه شده است. فایل `passenger_wsgi.py` به‌عنوان نقطه ورود استاندارد در ریشه پروژه قرار دارد و نیازی به پیکربندی پیچیده سرور ندارید.

---

## 🟢 روش اول (پیشنهادی): نصب گرافیکی با ابزار «Setup Python App» در سی‌پنل

اکثر هاست‌های سی‌پنل ایرانی و خارجی ابزار گرافیکی **Setup Python App** را در بخش *Software* دارند. با این ابزار، بدون نیاز به دانش دستورات لینوکس می‌توانید سایت را راه‌اندازی کنید.

### گام ۱: آپلود فایل‌های پروژه در سی‌پنل
1. وارد کنترل‌پنل **cPanel** شوید و ابزار **File Manager** را باز کنید.
2. به پوشه دامنه‌ای که می‌خواهید سایت روی آن باشد بروید:
   - برای دامنه اصلی: پوشه `public_html`
   - برای زیردامنه یا پوشه مجزا: مثلاً `public_html/lms` یا `/home/username/academy`
3. فایل زیپ پروژه را آپلود کرده و با کلیک راست و انتخاب گزینه **Extract**، فایل‌ها را از حالت فشرده خارج کنید.
   *(توجه: فایل `passenger_wsgi.py` و `app.py` باید مستقیماً در پوشه انتخابی شما باشند).*

---

### گام ۲: ساخت اپلیکیشن پایتون در سی‌پنل
1. در صفحه اصلی سی‌پنل، در بخش **Software** روی **Setup Python App** کلیک کنید.
2. روی دکمه **CREATE APPLICATION** کلیک کنید و فرم را مطابق زیر پر کنید:

| فیلد | مقدار مورد نظر | توضیح |
| :--- | :--- | :--- |
| **Python version** | `3.11` (یا `3.10`) | ترجیحاً نسخه 3.11 را انتخاب کنید |
| **Application root** | `public_html` (یا نام پوشه شما) | مسیری که فایل‌ها را آپلود کردید |
| **Application URL** | `yourdomain.com` | دامنه یا زیردامنه سایت |
| **Application startup file** | `passenger_wsgi.py` | **بسیار مهم:** حتماً همین نام نوشته شود |
| **Application Entry point** | `application` | **بسیار مهم:** نام متغیر WSGI در فایل پروژه |
| **Passenger log file** | `logs/passenger.log` (اختیاری) | جهت ثبت خطاهای احتمالی سرور |

3. روی دکمه **CREATE** در بالای صفحه کلیک کنید تا محیط پایتون ساخته شود.

---

### گام ۳: نصب پکیج‌های مورد نیاز (requirements.txt)
پس از ایجاد اپلیکیشن، در همان صفحه **Setup Python App** دو روش برای نصب کتابخانه‌ها وجود دارد:

* **روش الف (از داخل همان صفحه):**
  - در بخش **Configuration files** نام `requirements.txt` را بنویسید و دکمه **Add** را بزنید.
  - روی دکمه **Run Pip Install** که ظاهر می‌شود کلیک کنید و چند لحظه صبر کنید تا پیام موفقیت‌آمیز بودن نصب نشان داده شود.

* **روش ب (با ترمینال سی‌پنل - سریع‌تر):**
  - در بالای صفحه Setup Python App، دستوری به شکل زیر نمایش داده می‌شود:
    `source /home/username/virtualenv/public_html/3.11/bin/activate && cd /home/username/public_html`
  - آن دستور را کپی کنید، ابزار **Terminal** را در سی‌پنل باز کرده، دستور را پیست و Enter کنید.
  - سپس دستور زیر را اجرا کنید:
    ```bash
    pip install -r requirements.txt
    ```

---

### گام ۴: ساخت دیتابیس MySQL در سی‌پنل (اختیاری اما پیشنهادی برای پروداکشن)
اگر می‌خواهید از دیتابیس **MySQL** به جای SQLite استفاده کنید:
1. در سی‌پنل وارد **MySQL Database Wizard** شوید.
2. یک دیتابیس جدید بسازید (مثلاً `myhost_academy`).
3. یک کاربر دیتابیس با رمز عبور قوی بسازید (مثلاً `myhost_admin`).
4. در مرحله تعیین دسترسی‌ها، تیک **ALL PRIVILEGES** را بزنید.
5. *(پیشنهاد مهم)* در صورتی که دیتابیس را از phpMyAdmin می‌سازید، Collation دیتابیس را روی **`utf8mb4_unicode_ci`** قرار دهید تا کاراکترها و اسلاگ‌های فارسی بدون تداخل ذخیره شوند.
6. اطلاعات (نام دیتابیس، نام کاربری و رمز عبور) را یادداشت کنید تا در گام بعدی وارد کنید.

---

### گام ۵: اجرای نصب‌کننده وب و راه‌اندازی سایت
1. در صفحه **Setup Python App** در سی‌پنل، یک بار روی دکمه **RESTART** کلیک کنید.
2. آدرس سایت خود را در مرورگر باز کنید (مثلاً `https://yourdomain.com`).
   - سیستم به‌طور خودکار شما را به صفحه نصب‌کننده وب در مسیر `https://yourdomain.com/install` هدایت می‌کند.
3. در فرم نصب‌کننده ۳ مرحله‌ای (مشابه وردپرس):
   - **مرحله ۱ (دیتابیس):**
     - اگر MySQL ساختید: گزینه **MySQL** را انتخاب کرده و اطلاعات (آدرس host را `localhost` بگذارید) را وارد کنید و روی «تست اتصال» کلیک کنید.
     - اگر دیتابیس نساختید: گزینه **SQLite** را انتخاب کنید (بدون نیاز به هیچ تنظیمی).
   - **مرحله ۲ (مدیر سایت):** نام، ایمیل و رمز عبور حساب مدیریت سایت را تعیین کنید.
   - **مرحله ۳ (اطلاعات سایت):** نام سایت و تلفن تماس را بنویسید و روی **«⚡ شروع نصب و ساخت داده‌ها»** کلیک کنید.
   - نصب در چند درخواست کوتاه انجام می‌شود تا Passenger/cPanel آن را متوقف نکند. صفحه را تا انتقال خودکار نبندید. اگر ارتباط قطع شد، دکمه **«↻ ادامه نصب»** را بزنید؛ جدول‌های ساخته‌شده حفظ می‌شوند.

🎉 **تبریک!** سیستم تمام جدول‌ها، حساب مدیر، ۸ دسته‌بندی، دوره نمونه و تنظیمات اولیه را ساخته و سایت شما آنلاین می‌شود.

---

## 🟡 روش دوم: نصب با استفاده از فایل `.htaccess` و ترمینال (برای هاست‌های فاقد Setup Python App)

اگر هاست سی‌پنل شما ابزار گرافیکی `Setup Python App` ندارد اما از `Phusion Passenger` پشتیبانی می‌کند:

1. فایل‌های پروژه را در پوشه سایت (`public_html`) آپلود و اکسترکت کنید.
2. در سی‌پنل ابزار **Terminal** را باز کرده و دستورات زیر را اجرا کنید:
   ```bash
   cd ~/public_html
   python3 -m venv venv
   ./venv/bin/pip install -r requirements.txt
   ```
3. فایل نمونه `.htaccess` را از پوشه `deploy` به ریشه سایت کپی کنید:
   ```bash
   cp deploy/passenger.htaccess .htaccess
   ```
4. فایل `.htaccess` را در File Manager ویرایش کرده و مسیر واقعی هاست خود را قرار دهید:
   ```apache
   PassengerAppRoot /home/username/public_html
   PassengerPython  /home/username/public_html/venv/bin/python
   PassengerAppEnv  production
   ```
   *(به جای `username`، نام کاربری هاست سی‌پنل خود را قرار دهید).*
5. آدرس سایت را در مرورگر باز کرده و مراحل نصب را در `/install` انجام دهید.

---

## 🔄 نکات کاربردی و نگهداری سایت در سی‌پنل

### ۱. چگونه بعد از تغییر کدها، سایت را ری‌استارت کنیم؟
در هاست‌های اشتراکی سی‌پنل، بعد از آپدیت فایل‌های پایتون یا به‌روزرسانی از گیت، برای اعمال تغییرات دو راه دارید:
* **روش سریع با ترمینال / SSH:**
  ```bash
  touch ~/public_html/passenger_wsgi.py
  ```
* **روش گرافیکی:** در سی‌پنل به صفحه **Setup Python App** رفته و دکمه **RESTART** جلوی نام اپلیکیشن را بزنید.

### ۲. اتصال به گیت‌هاب از داخل پنل مدیریت سایت
پس از نصب، می‌توانید از داخل پنل ادمین سایت (`/admin/install-manager`) مسیر مخزن گیت‌هاب خود را تنظیم کنید و با یک کلیک روی **«🔄 دریافت کامل به‌روزرسانی از GitHub»**، تمام تغییرات جدید را روی هاست سی‌پنل خود دریافت کنید.

### ۳. تنظیم کرون جاب (Cron Job) برای بکاپ‌گیری و یادآوری‌ها (اختیاری)
سیستم دارای بکاپ‌گیری خودکار داخلی است که در هر درخواست بررسی می‌شود، اما برای اجرای وظایف دوره‌ای می‌توانید در بخش **Cron Jobs** سی‌پنل دستور زیر را برای اجرا هر شب ساعت ۳ بامداد (`0 3 * * *`) اضافه کنید:
```bash
/home/username/public_html/venv/bin/python /home/username/public_html/app.py --cron >/dev/null 2>&1
```

---

## 🛠️ عیب‌یابی خطاهای متداول نصب

### خطای «Duplicate entry '??????-?????' for key 'slug'»
- **علت:** این خطا زمانی رخ می‌دهد که انکودینگ (Collation) دیتابیس MySQL روی `latin1` تنظیم شده باشد. در این حالت حروف فارسی به علامت سؤال (`?`) تبدیل شده و اسلاگ‌های دسته‌ها و دوره‌ها تکراری می‌شوند.
- **راه‌حل:**
  1. در **phpMyAdmin**، وارد دیتابیس خود شوید و از تب **Operations** گزینه **Collation** را روی `utf8mb4_unicode_ci` قرار داده و دکمه **Go** را بزنید.
  2. در صورت وجود جدول‌های ساخته‌شده ناقص، همه جدول‌ها را در phpMyAdmin انتخاب و **Drop** کنید.
  3. به صفحه `/install` برگردید و دکمه شروع نصب را مجدداً بزنید. سیستم اکنون به‌صورت خودکار تمامی جدول‌ها را با `utf8mb4` می‌سازد.

