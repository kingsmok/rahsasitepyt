# 🚨 رفع هشدار «Dangerous site» کروم — راهنمای کامل

> پیامی که می‌بینید:
> **«Attackers on the site you tried visiting might trick you into installing
> software or revealing things like your passwords, phone, or credit card numbers.»**

## 🚨 علت اصلی پیدا شد: یک زیردامنهٔ فیشینگ روی دامنهٔ شما

**گواهی SSL شما کاملاً سالم است** (Let's Encrypt، معتبر تا ۶ نوامبر ۲۰۲۶) و
هیچ ربطی به این هشدار ندارد. مشکل جای دیگری بود.

با بررسی **Certificate Transparency** (فهرست عمومی تمام گواهی‌های صادرشده برای
هر دامنه) این را پیدا کردم:

```
xfinity.billing-account.update.diier.ir
```

| مورد | مقدار |
|---|---|
| تاریخ صدور گواهی | ۲۰ آگوست ۲۰۲۵ |
| صادرکننده | Let's Encrypt (R10) |
| سریال | 05950b167cbb250e545dfea013b7f9e1935b |
| وضعیت فعلی DNS | دیگر resolve نمی‌شود |
| وضعیت در Safe Browsing | **This site is unsafe** |

### چرا این فاجعه است؟

**Xfinity** برند اینترنت شرکت **Comcast** در آمریکاست. ساختار این آدرس:

`xfinity` . `billing-account` . `update` . `diier.ir`

یک الگوی **کلاسیک فیشینگ** است — صفحه‌ای که خود را «صفحهٔ بروزرسانی صورتحساب
اینترنت Xfinity» جا می‌زند تا از قربانی آمریکایی **شماره کارت اعتباری** بگیرد.

و این دقیقاً همان جمله‌ای است که کروم به شما نشان می‌دهد:
> "...trick you into revealing things like your passwords, phone, or **credit card numbers**"

### 🎯 نتیجه‌گیری قطعی

هر دو مورد زیر در Safe Browsing علامت خورده‌اند:
- `xfinity.billing-account.update.diier.ir` → unsafe
- `diier.ir` → unsafe

**علامتِ زیردامنه به کل دامنه سرایت کرده است.** یعنی سایت آموزشی شما به‌خاطر
کاری که روی یک زیردامنهٔ دیگر انجام شده، مسدود شده — نه به‌خاطر کد آکادمی.

⚠️ **این گواهی ۲۰ آگوست ۲۰۲۵ صادر شده** — یعنی حدود یک سال پیش و **قبل** از
اینکه سایت آکادمی فعلی بالا بیاید.

### ❓ سه حالت ممکن — کدام یک؟

1. **دامنه را دست‌دوم خریده‌اید** → مالک قبلی فیشینگ می‌زده. (اولین گواهی این
   دامنه دسامبر ۲۰۲۳ است، پس دامنه جدید نیست.)
2. **کسی به DNS یا هاست شما دسترسی داشته** → زیردامنه ساخته و فیشینگ زده.
3. **خودتان/همکارتان** آن زیردامنه را ساخته‌اید (بعید، ولی باید مطمئن شد).

---

## 🛠 کاری که باید انجام دهید (به ترتیب اولویت)

### گام ۱ — کنترل DNS را بررسی کنید 🔴 فوری

وارد پنل DNS دامنه (ایرنیک / پنل ثبت‌کننده / کلادفلر) شوید و **تمام رکوردها**
را ببینید. هر رکوردی که خودتان نساخته‌اید — مخصوصاً زیر `*.diier.ir` —
**فوراً حذف کنید**.

```
رکوردهایی که باید باشند:   diier.ir  →  87.236.209.235
                            www       →  87.236.209.235
هر چیز دیگر = مشکوک
```

### گام ۲ — رمزها را عوض کنید 🔴 فوری

اگر کسی توانسته زیردامنه بسازد، یعنی به یکی از این‌ها دسترسی داشته:
- پنل ثبت دامنه (ایرنیک)
- پنل DNS
- پنل هاست / سی‌پنل
- سرور (SSH)

**همه را عوض کنید** و تایید دومرحله‌ای را روشن کنید.

### گام ۳ — گواهی wildcard را جدی بگیرید

شما گواهی `*.diier.ir` دارید. این یعنی **هر زیردامنه‌ای** با همان گواهی کار
می‌کند. اگر کلید خصوصی لو رفته باشد، مهاجم می‌تواند هر زیردامنه‌ای بسازد.

توصیه: گواهی wildcard را **باطل (revoke)** و گواهی معمولی فقط برای
`diier.ir` و `www.diier.ir` بگیرید:

```bash
# باطل کردن گواهی فعلی
sudo certbot revoke --cert-name diier.ir
# گواهی جدید بدون wildcard
sudo certbot --nginx -d diier.ir -d www.diier.ir
```

### گام ۴ — سرور را برای فایل‌های جاماندهٔ فیشینگ بگردید

```bash
# پوشه‌هایی که مال آکادمی نیستند
ls -la /var/www/  /home/*/public_html/

# فایل‌های php مشکوک (پروژه پایتون است، نباید php داشته باشد!)
find / -name "*.php" -mtime -400 -not -path "*/node_modules/*" 2>/dev/null | head -40

# هر چیزی با نام xfinity/billing
find / -iname "*xfinity*" -o -iname "*billing*" 2>/dev/null | head -20

# vhost های اضافی
grep -rn "server_name" /etc/nginx/sites-enabled/ 2>/dev/null
```

### گام ۵ — بعد از پاک‌سازی، درخواست بازبینی

حالا که علت مشخص است، در درخواست بازبینی **دقیقاً همین را بنویسید**:

> A phishing subdomain (xfinity.billing-account.update.diier.ir) was created
> without my knowledge. I have removed the DNS record, revoked the wildcard
> certificate, rotated all credentials, and audited the server. The domain now
> only hosts a legitimate Persian e-learning site. Please re-review.

---

## 🔴 مهم‌ترین نکته که باید بدانید

این پیام **خطای کد سایت شما نیست** و با تغییر کد به‌تنهایی برطرف **نمی‌شود**.

این یک **علامت‌گذاری (flag) در سرویس Google Safe Browsing** است. یعنی گوگل دامنهٔ
شما را در فهرست سیاه خود ثبت کرده و کروم/فایرفاکس/اندروید آن را از همان فهرست
می‌خوانند. حتی اگر همین الآن کل کد را پاک کنید، تا وقتی از گوگل **درخواست بازبینی
(Request Review)** نکنید، هشدار باقی می‌ماند.

بنابراین کار در **دو مرحله** انجام می‌شود:

| مرحله | کار | وضعیت |
|-------|-----|-------|
| ۱ | بستن حفره‌هایی که باعث علامت خوردن شده‌اند | ✅ **در این بروزرسانی انجام شد** |
| ۲ | درخواست بازبینی از گوگل در Search Console | ⚠️ **باید خودتان انجام دهید** (راهنما پایین) |

---

## 🩺 مرحلهٔ صفر: عیب‌یابی خودکار

قبل از هر کاری، این دستور را **روی سرور خودتان** اجرا کنید:

```bash
python scripts/safe_browsing_check.py https://your-domain.ir
```

این اسکریپت دقیقاً می‌گوید کدام بخش مشکل دارد: فایل مشکوک آپلودشده، هدر گم‌شده،
`.htaccess` نصب‌نشده، شبیه‌ساز پرداخت باز، و... . خروجی آن را مبنای کار قرار دهید.

---

## 🔎 چرا گوگل سایت را علامت زده؟ (۵ علت رایج)

### ۱. فایل مخرب آپلودشده زیر دامنهٔ شما (شایع‌ترین علت)
اگر کسی توانسته باشد فایل `.php` یا `.html` آپلود کند، آن فایل با آدرس
`https://دامنه-شما/static/uploads/...` قابل دسترسی است. مهاجم لینک آن را پخش
می‌کند و گوگل می‌بیند که **دامنهٔ شما** صفحهٔ فیشینگ/بدافزار میزبانی می‌کند.

**بررسی کنید:**
```bash
find static/ -type f \( -name "*.php" -o -name "*.html" -o -name "*.svg" -o -name "*.js" \) \
  -newermt "2025-01-01" -not -path "*/icons/*" -ls
```
هر فایلی که خودتان آپلود نکرده‌اید = نفوذ. حذفش کنید و **همهٔ رمزها را عوض کنید**.

### ۲. صفحهٔ شبیه‌ساز پرداخت روی سایت واقعی
صفحه‌ای که ظاهر درگاه بانکی داشته باشد و در دسترس عموم باشد، مستقیماً
«Social Engineering» شناخته می‌شود. → در پنل، **حالت آزمایشی را خاموش کنید**.

### ۳. XSS ذخیره‌شده
اگر جایی HTML خام کاربر رندر شود، مهاجم `<script>` تزریق می‌کند و صفحهٔ شما
کاربران را به سایت جعلی می‌فرستد.

### ۴. Open Redirect
لینکی مثل `https://دامنه-شما/auth/login?next=https://سایت-جعلی` که کاربر به آن
اعتماد می‌کند ولی سر از سایت مهاجم درمی‌آورد.

### ۵. ادعای دروغین نماد اعتماد / درگاه بانکی
نمایش نماد اینماد بدون کد واقعی یا نوشتن «پرداخت امن شتاب» بدون داشتن درگاه،
از دید گوگل «محتوای فریب‌دهنده» است.

---

## ✅ چه چیزهایی در این بروزرسانی رفع شد

| # | حفره | اقدام انجام‌شده |
|---|------|-----------------|
| ۱ | آپلود فایل `html`/`js`/`css` توسط دانشجو | از فهرست مجاز حذف شد (`blueprints/features.py`) |
| ۲ | کتابخانهٔ رسانه هر فرمتی می‌پذیرفت | فقط تصویر/ویدیو/صوت/pdf (`ALLOWED_MEDIA_EXT`) |
| ۳ | SVG/تصویر حاوی `<script>` | بررسی **محتوای واقعی** فایل (`file_content_is_safe`) |
| ۴ | پسوند دوگانه `shell.php.jpg` | در `safe_filename` مسدود شد |
| ۵ | فایل آپلودی به‌صورت inline رندر می‌شد | اجباراً دانلود + `sandbox` + `nosniff` |
| ۶ | HTML خام صفحه‌ساز (`\|safe`) | پاکسازی با لیست سفید (`html_sanitizer.py`) |
| ۷ | `'unsafe-eval'` در CSP | حذف شد + `frame-ancestors`, `upgrade-insecure-requests` |
| ۸ | شناسهٔ GA/Clarity داخل `<script>` | فیلتر `tracking_id` (ضد تزریق) |
| ۹ | آدرس `/pay/bank/...` (شبیه صفحهٔ بانک) | به `/pay/sandbox/...` تغییر کرد |
| ۱۰ | ادعای «پرداخت امن شتاب» بدون درگاه | فقط با درگاه واقعیِ پیکربندی‌شده نمایش داده می‌شود |
| ۱۱ | پوشهٔ آپلود روی آپاچی محافظ نداشت | `.htaccess` خودکار نصب و ترمیم می‌شود |
| ۱۲ | `COEP: require-corp` منابع را می‌شکست | حذف شد (صفحات دیگر ناقص بارگذاری نمی‌شوند) |

برای همهٔ این موارد **۲۲ تست خودکار** نوشته شد (`tests/test_safe_browsing.py`)
تا در بروزرسانی‌های بعدی دوباره باز نشوند.

---

## 🛠 کارهایی که باید خودتان روی سرور انجام دهید

### گام ۱ — بروزرسانی و ری‌استارت
```bash
git pull
python scripts/safe_browsing_check.py https://your-domain.ir
# روی هاست اشتراکی:
touch passenger_wsgi.py
# روی سرور اختصاصی:
sudo systemctl restart academy
```

### گام ۲ — نصب محافظ پوشهٔ آپلود (هاست آپاچی/سی‌پنل)
اپ این کار را خودکار انجام می‌دهد، ولی برای اطمینان:
```bash
cp deploy/uploads.htaccess static/uploads/.htaccess
cp deploy/uploads.htaccess static/img/uploads/.htaccess
```

### گام ۳ — خاموش کردن حالت آزمایشی
پنل مدیریت → تنظیمات → **حالت آزمایشی: خاموش**
(یا در `.env` مقدار `ALLOW_SANDBOX_IN_PRODUCTION` را ست **نکنید**)

### گام ۴ — پاک‌سازی و تعویض رمزها
اگر نشانهٔ نفوذ دیدید:
```bash
# فایل‌های تغییریافتهٔ اخیر را ببینید
find . -type f -mtime -30 -not -path "./.git/*" -not -path "./.venv/*" -ls | head -50
```
سپس رمز پنل مدیریت، سی‌پنل/FTP، و دیتابیس را عوض کنید و در پنل
«خروج از همهٔ دستگاه‌ها» را بزنید.

### گام ۵ — HTTPS اجباری
بدون SSL معتبر، احتمال علامت خوردن بالا می‌رود:
```bash
sudo certbot --nginx -d your-domain.ir -d www.your-domain.ir
```

---

## 🟢 گام نهایی (مهم‌ترین): درخواست بازبینی از گوگل

بدون این مرحله هشدار **برطرف نمی‌شود** — حتی اگر سایت کاملاً تمیز باشد.

### راه سریع‌تر: بدون Search Console

اگر هنوز دامنه را به Search Console وصل نکرده‌اید، می‌توانید مستقیماً
«گزارش خطای تشخیص» بدهید:

🔗 <https://safebrowsing.google.com/safebrowsing/report_error/?hl=fa>

  • «نوع گزارش» را روی **«این صفحه امن است»** بگذارید
  • آدرس `https://diier.ir/` را وارد کنید
  • در توضیحات بنویسید چه چیزی را اصلاح کرده‌اید

⚠️ ولی **Search Console خیلی بهتر است**، چون تنها جایی است که گوگل
**دقیقاً می‌گوید کدام URL و چه نوع مشکلی** پیدا کرده. اتصال آن ۵ دقیقه است:

1. وارد <https://search.google.com/search-console> شوید
2. دامنهٔ خود را اضافه و مالکیت را تأیید کنید
   (روش ساده: فایل HTML یا رکورد DNS)
3. از منوی راست: **Security & Manual Actions → Security issues**
4. گوگل دقیقاً می‌نویسد چه چیزی پیدا کرده و **در کدام URL** —
   این ارزشمندترین اطلاعات برای شماست؛ آن URL را بررسی و پاک کنید
5. بعد از رفع، روی **REQUEST REVIEW** بزنید و توضیح دهید چه کرده‌اید
   (مثلاً: «فایل مخرب حذف شد، رمزها تغییر کرد، آپلود فایل اجرایی مسدود شد»)

⏱ **زمان بررسی گوگل:** معمولاً ۱ تا ۳ روز کاری (گاهی تا ۱۲ ساعت).

### بررسی وضعیت فعلی دامنه
<https://transparencyreport.google.com/safe-browsing/search?url=your-domain.ir>

---

## 📌 اگر Search Console چیزی نشان نداد

گاهی مشکل از خود سایت نیست:

- **هاست اشتراکی آلوده:** سایت دیگری روی همان IP آلوده است → از پشتیبانی هاست
  بخواهید بررسی کند، یا IP اختصاصی بگیرید
- **دامنهٔ دست‌دوم:** اگر دامنه قبلاً مال شخص دیگری بوده و سوءاستفاده شده،
  سابقهٔ بد آن باقی مانده → همان Request Review را بزنید
- **تبلیغات/اسکریپت شخص ثالث:** اسکریپت تبلیغاتی آلوده در سایت → حذفش کنید
- **فقط روی یک دستگاه:** افزونهٔ مخرب مرورگر یا DNS آلوده → با مرورگر/شبکهٔ
  دیگری تست کنید

---

## 🛡 پیشگیری از تکرار

```bash
# این تست‌ها را قبل از هر انتشار اجرا کنید
python -m pytest tests/test_safe_browsing.py tests/test_security.py -q
python scripts/safe_browsing_check.py https://your-domain.ir
```

- رمز مدیر قوی + تایید دومرحله‌ای (فعال است)
- هرگز افزونه/قالب از منبع نامعتبر نصب نکنید
- پشتیبان‌گیری منظم (پنل → پشتیبان‌گیری)
- دامنه را در Search Console نگه دارید تا هشدارها را زودتر ببینید
