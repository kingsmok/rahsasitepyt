# گزارش آمادگی عرضه تجاری

تاریخ ممیزی: ۱۴۰۵/۰۵/۲۶ (2026-08-17)

## دامنه بررسی

این مرحله روی چرخه نصب و انتشار، نقش‌ها و مجوزها، امنیت ورود مدیر، پنل مدرس، حالت‌های طراحی و جلوگیری از داده نمایشی در محیط واقعی متمرکز است.

## چرخه نصب و انتشار

- نصب تازه با `site_active=0` ساخته می‌شود و تا تایید مدیر برای بازدیدکننده عمومی بسته است.
- مدیر در حالت غیرفعال، پیش‌نمایش کامل سایت و پنل را در اختیار دارد.
- انتشار از «پنل مدیریت ← راه‌اندازی نهایی» انجام می‌شود.
- حداقل شرایط انتشار: هویت و راه تماس واقعی، حداقل یک محتوای منتشرشده، و برای محتوای پولی حداقل یک روش پرداخت پیکربندی‌شده.
- سایت کاملاً رایگان بدون درگاه قابل انتشار است.
- مدیر می‌تواند سایت را بدون تغییر یا حذف داده دوباره از دسترس عمومی خارج کند.
- نصب‌های قدیمی که کلید `site_active` ندارند، برای جلوگیری از قطعی ناگهانی فعال باقی می‌مانند.

## لایسنس فروش نرم‌افزار

- توکن لایسنس با Ed25519 امضا می‌شود و بدون کلید خصوصی فروشنده قابل جعل نیست.
- لایسنس می‌تواند به یک یا چند دامنه، wildcard زیردامنه، پلن، قابلیت و تاریخ انقضا متصل شود.
- کلید خصوصی فقط روی سیستم فروشنده نگهداری و کلید عمومی همراه بسته مشتری توزیع می‌شود.
- فعال‌سازی در `/license` انجام و فایل token با دسترسی محدود در `instance/license.json` ذخیره می‌شود.
- API در حالت بدون لایسنس پاسخ استاندارد HTTP 402 می‌دهد و صفحات وب به فعال‌سازی هدایت می‌شوند.
- callback پرداخت شروع‌شده از قفل لایسنس مستثناست تا تراکنش مشتری از بین نرود.
- نبود کلید عمومی، نصب‌های قدیمی را قفل نمی‌کند؛ `LICENSE_ENFORCEMENT=1` بدون public key به‌صورت fail-closed متوقف می‌شود.
- ZIP تجاری دارای `COMMERCIAL-BUILD.json` همیشه enforcement را فعال، fingerprint کلید عمومی را pin و در صورت حذف/تعویض کلید fail-closed عمل می‌کند.
- private key فروشنده می‌تواند با رمزی که فقط از متغیر محیطی خوانده می‌شود رمزگذاری شود؛ رمز در آرگومان command line قرار نمی‌گیرد.
- ابزار امن فروشنده: `scripts/license_tool.py` برای keygen، صدور و verify؛ سازنده ZIP: `scripts/build_commercial_package.py`؛ کنترل مستقل hash/path/secret/fingerprint: `scripts/verify_commercial_package.py`. سازنده پیش از تحویل، verifier مستقل را نیز خودکار اجرا می‌کند. ضعف ذاتی توزیع سورس نیز صادقانه باقی است: فرد دارای دسترسی کامل به کد می‌تواند کنترل محلی را patch کند و برای سخت‌گیری بیشتر باید اعتبارسنجی سروری/ماژول کامپایل‌شده جداگانه ارائه شود.

## امنیت نصب و مدیر

- مسیر تعمیر نصب و تعویض دیتابیس پس از نصب برای درخواست ناشناس قفل است.
- رمز و ایمیل مدیر پیش‌فرض از تعمیر حذف شده است.
- تعمیر اضطراری فقط با سشن سوپرادمین، رمز معتبر سوپرادمین یا `INSTALL_REPAIR_TOKEN` تصادفی فایل `.env` انجام می‌شود.
- مجوز موقت تعمیر ۱۵ دقیقه اعتبار دارد و پس از پایان عملیات حذف می‌شود.
- مسیر تعمیر rate-limit شده است.
- تا پیش از تایید کد دومرحله‌ای، وجود `uid` در سشن اجازه ورود مستقیم به `/admin` نمی‌دهد.
- انقضا یا عبور از سقف تلاش 2FA کل سشن نیمه‌کاره را باطل می‌کند.

## نقش‌ها

| نقش | دسترسی پیش‌فرض |
|---|---|
| سوپرادمین | همه بخش‌ها و عملیات حساس |
| ادمین | پنل کامل طبق Permissionهای مدیر |
| مدرس | دوره‌های اصلی و همکاری، دانشجویان، تکلیف، پرسش، درآمد و تسویه خود |
| منشی | کاربران، ثبت دانشجو، سفارش‌ها، مشاوره‌ها و کلاس‌های روزانه |
| پشتیبان | کاربران، دوره‌های کاربر، تیکت، پاسخ آماده و چت پشتیبانی |
| اپراتور | سفارش‌ها، تایید پرداخت و پیامک |
| دانشجو | حساب، یادگیری، سفارش‌ها، تیکت و امکانات دانشجویی |

نکات اجرایی:

- منوی هر نقش از همان Permission سمت سرور ساخته می‌شود؛ مخفی‌کردن لینک جایگزین کنترل دسترسی نیست.
- Permissionهای سفارشی ذخیره‌شده در پنل نقش‌ها در گارد route نیز اعمال می‌شوند.
- endpoint مدیریتی فاقد نگاشت برای کارکنان به‌صورت پیش‌فرض بسته است.
- داشبورد کارکنان از داشبورد مالی مدیر جداست و فقط شاخص مجاز را نشان می‌دهد.
- منشی فقط حساب دانشجو می‌سازد؛ اعطای نقش مدیریتی فقط توسط سوپرادمین ممکن است.
- تغییر نقش، غیرفعال‌سازی و ریست امنیتی باعث ابطال سشن قبلی کاربر می‌شود.

## مدرس و همکار مدرس

- دوره‌ای که مدرس در آن `CourseTeacher` است در همه صفحات پنل او یکسان دیده می‌شود.
- کنترل مالکیت تکلیف، پرسش، حضور و غیاب و دانشجو از یک منطق مشترک استفاده می‌کند.
- درآمد فقط از سفارش قطعی `paid` محاسبه می‌شود.
- مبلغ درخواست تسویه نمی‌تواند بیشتر از مانده قابل تسویه باشد.
- درخواست تسویه از حالت مشاهده مدیریتی ممکن نیست.

## ماتریس نمایش و دمو

تست خودکار موارد زیر را واقعاً رندر می‌کند:

- ۵ طرح صفحه اصلی
- ۵ طرح درباره ما
- ۵ طرح تماس با ما
- ۲۵ طرح کلی سایت
- ۲۰ واریانت کامل طراحی ایرانی
- وجود فایل CSS هر ۴۳ تم ثبت‌شده
- صفحات پنل همه نقش‌های عملیاتی

آمار fallback و اطلاعات تماس جعلی از مولد واریانت‌ها حذف شده است. دمو در production فعال نمی‌شود و نصب نهایی حساب یا محتوای نمایشی ایجاد نمی‌کند.

در نصب تجاری، پنل شناور انتخاب ۴۳ تم پیش‌فرض مخفی است تا هویت برند مشتری ثابت بماند. مدیر می‌تواند آن را برای سایت دمو از تنظیمات ظاهر فعال کند.

## ممیزی اجرای واقعی

خزش نقش‌محور جدید (`scripts/commercial_role_audit.py`) یک دیتابیس موقت کامل می‌سازد و لینک‌ها و منابع واقعی HTML را بررسی می‌کند. در این مرحله مواردی که تست‌های قدیمی ندیده بودند برطرف شد:

- چهار تصویر محصول 404 بود؛ داده seed اصلاح و چهار تصویر کاتالوگی واقعی اضافه شد.
- فاکتور PDF به فونت‌های TTF ناموجود وابسته بود؛ فونت محلی دارای مجوز، RTL صحیح و جدول چندصفحه‌ای اضافه شد.
- لینک آزمون و تمرین دوره‌های خریداری‌نشده به 403 می‌رسید؛ اکنون کارت قفل‌شده بدون لینک خراب نمایش داده می‌شود.
- لینک دعوت دوستان هنگام خاموش بودن قابلیت به 404 می‌رفت؛ لینک فقط در حالت فعال رندر می‌شود.
- بکاپ SQLite همیشه مسیر `instance/academy.db` را فرض می‌کرد؛ اکنون مسیر engine واقعی و API امن `sqlite3.backup` استفاده می‌شود.
- کتابخانه قالب صفحه‌ساز فایل runtime را در ریشه سورس می‌نوشت؛ به `instance/libraries` با نوشتن اتمیک، قفل و محدودیت حجم منتقل شد.
- مدیریت محصول اکنون آپلود امن تصویر، پیش‌نمایش و fallback فایل حذف‌شده دارد.
- صفحه‌ساز URLهای `javascript:`/protocol-relative، CSS تزریقی، شناسه‌های نامعتبر و payloadهای بیش‌ازحد را هنگام preview، ذخیره و کتابخانه قالب پاک‌سازی می‌کند.
- لینک‌ها و CTAهای بدون مقصد دیگر به `#` ختم نمی‌شوند و در خروجی عمومی به‌عنوان دکمه بی‌عمل رندر نمی‌شوند.
- نقشه ثابت تهران و ویدئوی نمونه از خروجی مشتری حذف شدند؛ نقشه فقط با نشانی واقعی به Google Maps متصل می‌شود و ZIP تجاری ویدئوی seed توسعه را بسته‌بندی نمی‌کند.

## نتیجه تست

```text
234 passed in 128.89s
Commercial role crawl: 1,176 pages + 5,715 static references, 0 issues
Commercial ZIP smoke: activate + HTTP 402/200 + health + manifest hashes, OK
Commercial ZIP audit: 840 entries, no private key/.env/instance/demo video
Live public crawl: 47 routes, 0 errors
Python compileall: OK
Python 3.8 syntax parse (89 files): OK
JavaScript syntax (7 files): OK
Jinja syntax (203 templates): OK
pip check: No broken requirements found
pip-audit: No known vulnerabilities found
Ruff fatal/undefined-name checks: OK
Bandit: 0 high-severity findings (medium findings manually reviewed)
Git diff check: OK
```

فرمان بازآزمایی:

```bash
python -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/pytest -q
```

## پیش‌نیازهای خارج از کد برای فروش واقعی

این موارد باید روی زیرساخت مقصد تکمیل و با حساب واقعی آزمایش شوند:

1. دامنه، DNS، SSL و اجبار HTTPS
2. قرارداد و اطلاعات پذیرندگی درگاه پرداخت
3. سرویس پیامک و قالب تاییدشده OTP
4. SMTP و سیاست SPF/DKIM/DMARC
5. اطلاعات حقوقی، قوانین بازگشت وجه و حریم خصوصی مخصوص کسب‌وکار
6. بکاپ زمان‌بندی‌شده خارج از همان سرور و آزمون بازیابی
7. مانیتورینگ، Redis و تنظیم ظرفیت MySQL متناسب با ترافیک
8. تست نهایی پرداخت واقعی کم‌مبلغ و callback روی دامنه production
