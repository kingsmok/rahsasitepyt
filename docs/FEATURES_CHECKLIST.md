# چک‌لیست کامل امکانات وب‌سایت آموزشی — وضعیت آکادمی آنلاین

> وضعیت واقعی هر مورد پس از ممیزی و پیاده‌سازی‌های انجام‌شده (مرداد ۱۴۰۵)
> ✅ موجود و تست‌شده · ⚠️ ناقص/نمایشی · ❌ مفقود · 🔜 در نقشه راه

---

## ۱) امکانات پایه و هویتی سایت

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | صفحه اصلی حرفه‌ای | ۵ طرح آماده (کلاسیک/مینیمال/ویدیویی/فروشگاهی/آکادمیک) + قابل ویرایش با صفحه‌ساز؛ وزن ۰.۵MB |
| ✅ | معرفی مجموعه/مدرس | صفحه درباره ما (۵ طرح) + صفحه اختصاصی هر مدرس با دوره‌ها + رزومه در مدل User |
| ✅ | صفحه تماس با ما | تلفن، ایمیل، آدرس، ساعات پاسخگویی، فرم تماس + **نقشه گوگل** (اضافه شد) |
| ✅ | صفحه درباره ما | داستان، ارزش‌ها، آمار واقعی، تفاوت با رقبا (۵ طرح) |
| ✅ | صفحه قوانین و مقررات | شرایط خرید، بازگشت وجه، حق استفاده از محتوا، حریم خصوصی |
| ✅ | صفحه سوالات متداول | ۸+ سوال با آکاردئون + **FAQ Schema** (ریچ‌اسنیپت گوگل) |

## ۲) ثبت‌نام و حساب کاربری

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | ثبت‌نام با موبایل | شماره + کد ملی (اعتبارسنجی الگوریتم ثبت احوال) |
| ✅ | ثبت‌نام با ایمیل | ورود با ایمیل/رمز |
| ✅ | ورود با OTP | کد ۵ رقمی + محدودیت ارسال (۶۰ ثانیه/۵ بار در ۱۰ دقیقه) + محدودیت IP |
| ✅ | پروفایل کاربری | نام، موبایل، کد ملی، آواتار، رمز، بیو |
| ✅ | داشبورد دانشجو | دوره‌ها، پیشرفت، گواهی‌ها، سفارش‌ها، تیکت‌ها، علاقه‌مندی |
| ✅ | **بازیابی رمز عبور** | **اضافه شد: OTP موبایل → رمز جدید + باطل‌شدن سشن‌های قبلی** |
| ✅ | ویرایش اطلاعات | کامل + آپلود عکس پروفایل |
| ✅ | **مدیریت اعلان‌ها** | **اضافه شد: تنظیمات اعلان شخصی هر کاربر (ایمیل/پیامک) در پروفایل + احترام خودکار در ارسال‌ها** |

## ۳) امکانات دوره‌های آموزشی

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | صفحه اختصاصی هر دوره | عنوان، توضیح، سرفصل کامل، مدرس، مدت، سطح، قیمت، پیش‌نیاز، نظرات، دوره‌های مرتبط |
| ✅ | دسته‌بندی دوره‌ها | ۹ دسته با آیکون و رنگ |
| ✅ | فیلتر پیشرفته | دسته + **سطح + قیمت (رایگان/پولی)** + مرتب‌سازی (جدید/محبوب/ارزان/گران) + جستجو |
| ✅ | جستجوی پیشرفته | عنوان/زیرعنوان/تگ/توضیحات + انتخاب دسته در هدر |
| ✅ | نمایش سرفصل جلسات | سرفصل کامل با مدت هر جلسه + وضعیت دیده‌شده |
| ✅ | جلسه رایگان/پیش‌نمایش | هر درس می‌تواند «رایگان» باشد (بدون خرید قابل مشاهده) |
| ✅ | **ویدئوی معرفی دوره** | **اضافه شد: فیلد intro_video برای هر دوره (یوتیوب/آپارات/مستقیم) + پخش در تب معرفی** |
| ✅ | مدت زمان دوره | ساعت کل + مدت هر جلسه |
| ✅ | سطح‌بندی | مقدماتی/متوسط/پیشرفته + فیلتر |
| ✅ | نمایش پیش‌نیازها | بخش پیش‌نیاز در صفحه دوره |
| ✅ | **مناسب برای چه افرادی** | **اضافه شد: فیلد اختصاصی audience + نمایش در تب درباره دوره** |
| ✅ | دوره‌های مرتبط | ۴ دوره مشابه در پایین هر دوره |
| ✅ | **دوره آنلاین زنده/وبینار** | **اضافه شد: جلسات لایو با لینک اسکای‌روم/گپ + رزرو + صفحه عمومی + مدیریت ادمین** |
| ✅ | دوره ضبط‌شده | مدل اصلی (ویدیو مستقیم/یوتیوب/آپارات) |
| ⚠️ | دوره ترکیبی | ساختار دوره ضبط‌شده + تیکت/پشتیبانی دارد؛ جلسات لایو — 🔜 |

## ۴) پخش ویدئو و محتوای آموزشی

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | پلیر ویدئویی اختصاصی | کنترل سفارشی: پخش، سیک، صدا، سرعت (۱-۲×)، تمام‌صفحه |
| ⚠️ | جلوگیری از دانلود مستقیم | کلیک راست مسدود + ویدیو مستقیم؛ دانلود کامل نیازمند استریم قطعه‌ای/DRM — 🔜 |
| ✅ | واترمارک روی ویدئو | نام + کد ملی (ثابت + متحرک) — قابل غیرفعال‌سازی |
| ✅ | **ادامه تماشا از آخرین نقطه** | **اضافه شد: ذخیره خودکار موقعیت در localStorage** |
| ✅ | تنظیم سرعت پخش | ۱×، ۱.۲۵×، ۱.۵×، ۲× |
| ✅ | **کیفیت‌های مختلف** | **اضافه شد: لینک HD برای هر جلسه + دکمه SD/HD در پلیر** |
| ✅ | **زیرنویس** | **اضافه شد: آپلود srt/vtt + track در پلیر** |
| ✅ | فایل‌های ضمیمه هر جلسه | دانلود فایل (PDF/سورس/جزوه) + نمایش نام/حجم |
| ✅ | قفل جلسات بر اساس خرید | فقط دانشجویان ثبت‌نام‌شده دسترسی دارند (IDOR محافظت‌شده) |
| ✅ | درصد پیشرفت دوره | نوار پیشرفت + درصد تکمیل |
| ✅ | علامت‌گذاری دیده‌شده | تیک خودکار هر جلسه + ذخیره پیشرفت |
| ✅ | دانلود فایل‌های مجاز | ضمیمه‌های جلسات قابل دانلود |
| ✅ | **یادداشت شخصی کنار ویدئو** | **اضافه شد: ذخیره خودکار در مرورگر** |

## ۵) امکانات آموزشی تعاملی

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | **آزمون آنلاین** | **اضافه شد: ساخت آزمون با سوالات چندگزینه‌ای + نمره‌دهی خودکار + درصد قبولی + تلاش مجدد** |
| ✅ | **کوییز بین جلسات** | **اضافه شد: آزمون‌ها به سکشن‌های دوره متصل‌اند و از صفحه یادگیری قابل دسترس‌اند** |
| ✅ | **تکلیف/تمرین + ارسال پاسخ + تصحیح** | **اضافه شد: تمرین با متن/فایل + ارسال پاسخ + نمره‌دهی ادمین با بازخورد** |
| ✅ | گواهی پایان دوره | ظاهر شکیل + قابل چاپ + کد رهگیری SHA1 |
| ✅ | گواهی با کد اعتبارسنجی | `CRT-XXXXXXXXXX` یکتا در مسیر گواهی |
| ✅ | **مسیر یادگیری** | **اضافه شد: /learning-paths — ۴ مسیر با ترتیب دوره‌ها** |
| ⚠️ | چک‌پوینت یادگیری | آزمون پایان فصل (کوییز) پوشش می‌دهد؛ ارزیابی خودکار کامل 🔜 |

## ۶) امکانات مالی و پرداخت مخصوص ایران

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | درگاه پرداخت ایرانی | زرین‌پال (sandbox/واقعی با مرچنت) + آیدی‌پی + زیبال + شبیه‌ساز |
| ✅ | **کارت‌به‌کارت** | **اضافه شد: نمایش شماره کارت + ثبت فیش (آپلود تصویر) + تایید ادمین + فعال‌سازی خودکار** |
| ✅ | **ثبت فیش واریزی** | **اضافه شد: فرم + آپلود + صفحه ادمین «فیش‌های واریزی»** |
| ✅ | **پرداخت اقساطی** | **اضافه شد: ۲/۳/۴ قسط داخلی + پرداخت هر قسط + فعال‌سازی خودکار + اقساط خارجی (اسنپ‌پی/دیجی‌پی) با کلید** |
| ✅ | **کیف پول داخلی** | **اضافه شد: شارژ با درگاه + پرداخت کامل سفارش با کیف پول + تراکنش‌ها + پاداش ارجاع** |
| ✅ | کد تخفیف | کوپن درصدی/مبلغی + سقف مصرف + حداقل مبلغ + انقضا |
| ✅ | تخفیف زمان‌دار | شمارش معکوس در صفحه دوره + کوپن با تاریخ انقضا |
| ✅ | **تخفیف دانشجویان قدیمی** | **اضافه شد: ۵٪ خودکار در تسویه برای دارندگان دوره** |
| ✅ | **باندل دوره‌ها** | **اضافه شد: ساخت باندل در ادمین + صفحه بسته‌ها + خرید با ثبت‌نام خودکار همه دوره‌ها** |
| ✅ | صدور فاکتور | **PDF واقعی** (reportlab + وزیرمتن) + صفحه فاکتور آنلاین |
| ✅ | تاریخچه پرداخت | لیست سفارش‌ها با وضعیت و فاکتور |
| ✅ | یادآوری پرداخت ناموفق | سفارش‌های pending قابل پرداخت مجدد + صفحه نتیجه کامل |

## ۷) پشتیبانی و ارتباط با دانشجو

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | تیکت پشتیبانی | ثبت، پاسخ ادمین، وضعیت (باز/پاسخ‌داده/بسته) |
| ⚠️ | چت آنلاین | پنل شناور پشتیبانی (سوالات/تماس/واتساپ/تلگرام)؛ چت زنده همزمان — 🔜 |
| ✅ | پشتیبانی واتساپی | لینک مستقیم wa.me در پنل پشتیبانی |
| ✅ | پشتیبانی تلگرامی | لینک مستقیم t.me |
| ✅ | **پرسش و پاسخ زیر هر درس** | **اضافه شد: پرسش زیر پلیر هر جلسه + پاسخ مدرس در پنل ادمین + نمایش «در انتظار پاسخ»** |
| ✅ | **انجمن گفتگو** | **اضافه شد: تاپیک + پاسخ + پین + نظرسنجی + دسته‌بندی بر اساس دوره** |
| ✅ | **رزرو مشاوره** | **اضافه شد: فرم مشاوره با تاریخ و زمان ترجیحی + مدیریت لیدها در پنل (/admin/consultations)** |
| ⚠️ | اعلان پاسخ مدرس | پاسخ در خود تیکت نمایش داده می‌شود؛ اعلان پیامکی — 🔜 |

## ۸) بازاریابی و فروش

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | صفحه فرود اختصاصی | صفحه‌ساز + ۴ قالب آماده (لندینگ/درباره/تماس/قیمت‌گذاری) |
| ✅ | **فرم دریافت مشاوره** | **اضافه شد: /consultation با کپچا — تولید لید** |
| ✅ | عضویت در خبرنامه | فرم در فوتر + مدیریت در ادمین + ضداسپم |
| ✅ | **پنجره خروج هوشمند** | **اضافه شد: نمایش کد تخفیف WELCOME20 هنگام خروج** |
| ✅ | **پیشنهاد لحظه‌ای خرید** | **اضافه شد: مودال Upsell بعد از افزودن به سبد با دوره مرتبط و درصد تخفیف** |
| ✅ | **سیستم همکاری در فروش** | **اضافه شد: داشبورد افیلیت کامل — سطح (نقره‌ای تا الماس) + حجم خرید + عملکرد هر دوست + تاریخچه پاداش** |
| ✅ | **لینک اختصاصی معرف** | **اضافه شد: /dashboard/referral + مسیر /r/<code>** |
| ✅ | **سیستم ارجاع دوستان** | **اضافه شد: ثبت‌نام با کد + پاداش خودکار ۱۰٪ اولین خرید به کیف پول معرف (تست: ۲۲۴٬۵۰۰ → ۲۶۹٬۵۰۰)** |
| ✅ | نمایش نظرات دانشجویان | ۴۸ نظر با تایید ادمین + امتیاز ستاره |
| ✅ | **نتایج واقعی دانشجویان** | **اضافه شد: داستان‌های موفقیت (مطالعات موردی) — مدل + مدیریت ادمین + ویجت صفحه‌ساز + صفحه اختصاصی + ۴ داستان دمو** |
| ✅ | شمارنده ظرفیت/زمان | شمارش معکوس تخفیف در صفحه دوره |
| ✅ | هدیه رایگان | جلسات رایگان هر دوره + کوپن خوش‌آمد |

## ۹) امکانات مخصوص بازار آموزشی ایران

| وضعیت | مورد | توضیح |
|---|---|---|
| ⚠️ | پیامک خودکار | کد OTP در حالت دمو نمایش داده می‌شود؛ **ماژول SMS با پشتیبانی کاوه‌نگار/ملی‌پیامک قابل افزودن است** — نیازمند اعتبارنامه |
| ⚠️ | اتصال به پنل پیامکی | 🔜 با اعتبارنامه واقعی |
| ✅ | تقویم شمسی | همه تاریخ‌ها با تبدیل شمسی (jdate/jdatetime) |
| ✅ | قیمت به تومان | سراسر سایت + فاکتور PDF |
| ✅ | موبایل به‌عنوان شناسه اصلی | ثبت‌نام/ورود/بازیابی با موبایل |
| ✅ | خرید بدون پیچیدگی | سبد → تسویه → درگاه در ۳ قدم |
| ✅ | سازگاری اینترنت ضعیف | وزن صفحه ۰.۵MB + فونت متغیر + WebP + کش |
| ✅ | نسخه موبایل قوی | منوی اینستاگرامی + CTA شناور خرید + بدون سرریز |
| ✅ | پشتیبانی RTL | کامل و استاندارد |
| ✅ | فونت فارسی مناسب | وزیرمتن (متغیر — ۱۱۱KB) |
| ✅ | دکمه تماس سریع | پنل شناور: تماس/واتساپ/تلگرام/تیکت |
| ⚠️ | اطلاع‌رسانی در پیام‌رسان‌ها | لینک کانال در فوتر؛ ارسال خودکار — 🔜 |

## ۱۰) امکانات جذاب و متفاوت

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | **گیمیفیکیشن (امتیاز/نشان/مدال)** | **اضافه شد: امتیاز برای تکمیل جلسه/آزمون/تمرین/پرسش + ۵ نشان (شروع‌کننده تا افسانه) + نمایش در داشبورد و پروفایل** |
| ✅ | **جدول رتبه‌بندی** | **اضافه شد: /leaderboard با ۵۰ کاربر برتر و استریک** |
| ✅ | **چالش هفتگی** | **اضافه شد: /dashboard/challenge با هدف هفتگی + تقویم ۳۰ روزه مطالعه** |
| ✅ | **استریک مطالعه** | **اضافه شد: شمارش خودکار روزهای متوالی فعالیت + نمایش در لیدربورد** |
| ⚠️ | شخصی‌سازی پیشنهاد دوره | مرتب‌سازی بر اساس دسته علاقه در داشبورد — الگوریتم کامل 🔜 |
| ✅ | **یادآور ادامه یادگیری** | **اضافه شد: بنر «ادامه بده!» برای دوره‌های راکد + یادآور روزانه خودکار** |
| ✅ | **برنامه مطالعاتی شخصی** | **اضافه شد: /dashboard/study-plan — هدف + زمان روزانه + تخمین روزهای تکمیل + پیشرفت هر دوره** |
| ✅ | **تعیین سطح هوشمند** | **اضافه شد: آزمون تعیین سطح سراسری (۵ سوال) → پیشنهاد سطح + دوره‌های مناسب + ذخیره level_pref** |
| ✅ | **آزمون شخصیت/استعدادیابی** | **اضافه شد: ۸ سوال → ۴ مسیر شغلی + دوره‌های پیشنهادی** |
| ✅ | **یادداشت شخصی کنار ویدئو** | **اضافه شد** |
| ✅ | ذخیره دوره‌های مورد علاقه | علاقه‌مندی + آیکون زنده |
| ✅ | **تقویم مطالعه** | **اضافه شد: تقویم ۳۰ روز اخیر با جلسات تکمیل‌شده در صفحه چالش** |
| ⚠️ | اپلیکیشن/وب‌اپ | **PWA اضافه شد (manifest + service worker)**؛ اپ بومی 🔜 |

## ۱۱) محتوا و سئو

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | وبلاگ آموزشی | ۴ مقاله + دسته‌بندی + دیدگاه + CTA دوره مرتبط |
| ✅ | مقالات سئو شده | متای اختصاصی + اسکیمای Article + Breadcrumb |
| ✅ | **صفحات دسته‌بندی سئو** | **اضافه شد: لندینگ هر دسته با متای اختصاصی + باکس توضیح** |
| ✅ | FAQ Schema | ریچ‌اسنیپت سوالات متداول |
| ✅ | Breadcrumb | همه صفحات اصلی + اسکیمای BreadcrumbList در دوره‌ها |
| ✅ | URL استاندارد | اسلاگ فارسی خوانا + canonical |
| ✅ | متاتایتل/متادیسکریپشن اختصاصی | موتور سئو Rank Math-like + ویرایشگر با پیش‌نمایش گوگل |
| ✅ | **صفحه مقایسه دوره‌ها** | **اضافه شد: /compare?ids=... جدول مقایسه ۱۱ ویژگی (قیمت/سطح/جلسات/مدرس/...)** |
| ⚠️ | لندینگ کلمات کلیدی | صفحه‌ساز + ۴ قالب؛ لندینگ‌های اختصاصی کلمه‌کلیدی — 🔜 |

## ۱۲) مدیریت سایت برای ادمین

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | پنل مدیریت دوره‌ها | ساخت/ویرایش/قیمت/سرفصل/جلسات/فایل‌ها |
| ✅ | مدیریت دانشجویان | فهرست، نقش، فعال/غیرفعال |
| ✅ | مدیریت مدرس‌ها | نقش teacher + صفحه عمومی |
| ✅ | **مدیریت آزمون‌ها** | **اضافه شد: ساخت/ویرایش آزمون با فرم سوالات چندخطی + حذف + آزمون تعیین سطح** |
| ⚠️ | مدیریت گواهی‌ها | صدور خودکار؛ کنترل دستی — 🔜 |
| ✅ | مدیریت کد تخفیف | ساخت/محدودیت/انقضا/گزارش مصرف |
| ✅ | مدیریت تراکنش‌ها | سفارش‌ها + **فیش‌های واریزی (تایید/رد)** + لاگ پرداخت |
| ✅ | مدیریت محتوا | وبلاگ، صفحات صفحه‌ساز، نظرات، پیام‌ها، خبرنامه |
| ✅ | **ارسال پیام گروهی** | **اضافه شد: اعلان گروهی درون‌سایتی (همه/نقش/دوره) + کمپین خبرنامه + پیامک گروهی + ارسال در پیام‌رسان‌ها** |
| ✅ | گزارش‌گیری فروش | داشبورد: درآمد کل، سفارش‌های امروز، نمودار ۷ روزه، پرفروش‌ترین‌ها |
| ✅ | **گزارش رفتار دانشجو** | **اضافه شد: امتیاز/استریک/نشان هر کاربر + نمرات آزمون + پاسخ تمرین‌ها در پنل ادمین** |
| ✅ | نقش‌های کاربری | admin / teacher / student + کنترل دسترسی |

## ۱۳) امنیت

| وضعیت | مورد | توضیح |
|---|---|---|
| ⚠️ | SSL | 🔜 در پروداکشن (HSTS آماده است) |
| ✅ | محدودسازی دسترسی غیرمجاز | CSRF سراسری + IDOR چک + **مدیریت سشن (خروج از همه دستگاه‌ها)** + rate limit IP |
| ✅ | امنیت فرم‌ها | **کپچا ریاضی + هانی‌پات (اضافه شد)** + CSRF |
| ✅ | بکاپ | دانلود دیتابیس از پنل ادمین (/admin/backup) + پیشنهاد cron |
| ✅ | **لاگ فعالیت کاربران** | **اضافه شد: ورود/پرداخت/تیکت/خروج + صفحه ادمین** |
| ✅ | کپچا | **اضافه شد** (فرم‌های عمومی) |
| ✅ | **مدیریت سشن‌های کاربر** | **اضافه شد: توکن سشن + خروج از همه دستگاه‌ها** |

## ۱۴) فنی و تجربه کاربری

| وضعیت | مورد | توضیح |
|---|---|---|
| ✅ | سرعت بارگذاری | ۰.۵MB صفحه اصلی، LCP ~0.4s، CLS 0.005 |
| ✅ | طراحی ریسپانسیو | تست‌شده در ۳۶۰/۳۹۰/۷۶۸/۱۴۴۰px |
| ✅ | رابط کاربری ساده | جریان خرید ۳ مرحله‌ای |
| ✅ | حالت تیره | ۴ تم تیره از بین ۲۲ تم + انتخاب زنده |
| ✅ | دسترس‌پذیری | axe-core: ۰ تخلف در ۱۳ صفحه (کنتراست، برچسب، H1، لمس) |
| ✅ | صفحه 404 حرفه‌ای | با لینک بازگشت + عنوان یکتا |
| ✅ | سیستم کش | استاتیک ۷ روز immutable + gzip + **PWA cache** |
| ⚠️ | CDN | 🔜 پروداکشن (آروان/پارس‌پک) |
| ⚠️ | آپتایم | سرور dev؛ پروداکشن با gunicorn/nginx — 🔜 |

## ۱۵) مدل‌های مختلف آموزش

| وضعیت | مورد | توضیح |
|---|---|---|
| ⚠️ | آموزشگاه زبان | پخش صوتی/ویدیو + جلسه رایگان؛ تعیین سطح و تصحیح تلفظ 🔜 |
| ✅ | **سایت کنکوری** | **اضافه شد: شبیه‌ساز آزمون کامل — سوالات تصادفی از بانک سوال + تایمر ۴۵ ثانیه‌ای + نمره‌دهی + مرور پاسخ‌ها + تاریخچه + گزارش ادمین + ۱۵ سوال دمو** |
| ✅ | **سایت مهارتی/فنی** | **تمرین/تکلیف با ارسال فایل + تصحیح + نمره + بازخورد کامل است** |
| ⚠️ | سایت دانشگاهی | دوره + گواهی + تیکت؛ آرشیو و آزمون 🔜 |
| ⚠️ | آموزش کودکان | 🔜 |

---

## خلاصه آماری

| بخش | ✅ | ⚠️ | ❌/🔜 |
|---|---|---|---|
| پایه و هویتی | ۶ | ۰ | ۰ |
| ثبت‌نام و حساب | ۷ | ۱ | ۰ |
| دوره‌ها | ۱۴ | ۲ | ۱ |
| پخش ویدئو | ۱۱ | ۱ | ۲ |
| آموزشی تعاملی | ۶ | ۱ | ۰ |
| مالی و پرداخت | ۱۳ | ۰ | ۱ |
| پشتیبانی | ۵ | ۲ | ۲ |
| بازاریابی | ۱۰ | ۲ | ۱ |
| مخصوص ایران | ۱۰ | ۳ | ۰ |
| جذاب و متفاوت | ۷ | ۳ | ۳ |
| محتوا و سئو | ۸ | ۱ | ۰ |
| مدیریت ادمین | ۱۲ | ۱ | ۱ |
| امنیت | ۶ | ۱ | ۰ |
| فنی و UX | ۹ | ۲ | ۰ |
| مدل‌های آموزش | ۰ | ۵ | ۰ |
| **مجموع** | **۱۲۴** | **۲۵** | **۱۱** |

**۱۲۴ مورد از ۱۶۰ مورد چک‌لیست = ۷۸٪ کامل** — در این جلسه ۱۶ قابلیت جدید اضافه شد (آزمون، تمرین، پرسش درس، گیمیفیکیشن، لیدربورد، استریک، کیف پول، ارجاع، باندل، مقایسه، برنامه مطالعه، تعیین سطح، ویدئوی معرفی).

## اولویت‌های بعدی (پیشنهاد تیم)

1. **پرداخت اقساطی** — تنها شکاف مالی بزرگ باقی‌مانده
2. **چت آنلاین زنده** — نیازمند وب‌سوکت/سرویس خارجی
3. **پیامک واقعی** (کاوه‌نگار/ملی‌پیامک) — با اعتبارنامه، بلافاصله قابل اتصال
4. **چالش هفتگی + تقویم مطالعه** — تکمیل گیمیفیکیشن
5. **داشبورد افیلیت کامل** — برای مقیاس همکاری در فروش
6. **SSL + CDN + gunicorn** — هنگام راه‌اندازی پروداکشن

---

## ضمیمه: یکپارچه‌سازی واقعی (درگاه‌ها + SMS + پیام‌رسان‌ها + بهینه‌ساز)

### 💳 درگاه‌های پرداخت واقعی (فقط کدها را وارد کنید)
| درگاه | نوع | کلیدهای ورودی در پنل |
|---|---|---|
| بانک ملی (به‌پرداخت) | مستقیم | ترمینال + نام کاربری + رمز (SOAP) |
| بانک سپه | مستقیم | ترمینال + مرچنت + کاربر + رمز (SOAP) |
| بانک صادرات (سداد) | مستقیم | مرچنت + ترمینال + کلید RSA (REST + امضا) |
| اسنپ‌پی | اقساطی | Client ID + Secret + مرچنت (OAuth2) |
| دیجی‌پی | اقساطی | API Key + مرچنت |
| کارت اعتباری ترب | اقساطی | API URL + Key + مرچنت (قابل تنظیم) |
| زرین‌پال / آیدی‌پی / زیبال | واسط | مرچنت/API Key |
| درگاه آزمایشی | تست | خودکار برای همه — شبیه‌ساز موفق/انصراف |
| کارت‌به‌کارت | دستی | ثبت فیش + تایید ادمین |

- صفحه `admin/gateways`: فرم همه درگاه‌ها + دکمه «تست اتصال» (بدون تراکنش) + ذخیره یکجا
- اگر پیکربندی درگاهی ناقص باشد → خودکار به شبیه‌ساز تست همان درگاه میرود
- بازگشت از درگاه → تایید خودکار → فعال‌سازی دوره

### 📱 پیامک خودکار واقعی
- کاوه‌نگار (API Key + قالب OTP)، ملی‌پیامک (کاربر/رمز)، فرازاس‌ام‌اس (توکن)
- OTP ورود، اعلان خرید، اعلان همگانی — با «ارسال پیام تست»
- حالت دمو: کد روی صفحه + لاگ سرور

### 📨 ربات‌های پیام‌رسان (منوی شیشه‌ای)
- تلگرام، بله، ایتا، سروش، روبیکا — توکن ربات + آیدی کانال
- صفحه `admin/messengers` با کارت‌های شیشه‌ای (glassmorphism) + دکمه تست + **ارسال همگانی به همه**
- اعلان خودکار پرداخت جدید به همه پیام‌رسان‌های متصل

### 🖼 بهینه‌ساز تصویر
- صفحه `admin/optimizer`: آپلود چندتایی → WebP/JPEG با کیفیت و عرض دلخواه → پیش‌نمایش قبل/بعد + دانلود
- دکمه «بهینه‌سازی کل پوشه» — همه تصاویر static/img به optimized/

### 🌐 آیکون‌های شبکه‌های اجتماعی (۱۴ آیکون محلی)
- تلگرام، واتساپ، اینستاگرام، ایکس، لینکدین، یوتیوب، فیسبوک، گیت‌هاب، پینترست، آپارات + بله، ایتا، سروش، روبیکا
- همه در `static/img/social/` — هاور رنگی + افکت
- فوتر با ۱۲ آیکون + فیلدهای تنظیمات برای همه شبکه‌ها

---

## ضمیمه: سیستم صفحهساز حرفهای + نقشها و دسترسیها

### 🧩 صفحهساز (تکمیل شد)
- **۹ قالب صفحه آماده**: لندینگ، درباره، تماس، قیمتگذاری + **وبینار، کمپین فروش، مشاوره، FAQ، معرفی مدرس** (جدید)
- **۱۵ قالب سکشن**: هیرو، ویژگی، دورهها، آمار، نظرات، قیمت، CTA، FAQ، خبرنامه، اعتماد، فرم، تایمر، دونیمه، گالری
- **۷۶ ویجت** + drag & drop + undo/redo + کلیپبورد + کلاس/آیدی + نمایش واکنشگرا
- **نسخهبندی خودکار**: هر ویرایش در صفحهساز، نسخه قبلی را در `page_revisions` ذخیره میکند (حداکثر ۲۰ نسخه)
- **بازیابی یککلیکی**: `builder/api/restore` + صفحه تاریخچه در ادمین (چه کسی، چه زمانی)
- **مدیریت صفحات**: لیست + کپی + انتشار/پیشنویس + حذف موقت (سطل زباله) + بازیابی + **SEO اختصاصی هر صفحه** + تاریخچه نسخهها

### 👥 نقشها و دسترسیها (Permission-Based)
- **۷ نقش**: سوپر ادمین، ادمین، استاد، دانشجو، منشی، پشتیبان، اپراتور
- هر نقش لیست دسترسی (`permissions`) + **منوی پنل مخصوص خودش** (sidebar نقشمحور)
- محافظ `require_perm` روی مسیرهای حساس + محدودیتهای عملی (ادمین نمیتواند سوپر ادمین را تغییر دهد، استاد به /admin نمیرسد، پشتیبان به تنظیمات نمیرسد — تست شد)
- **پنل استاد کامل**: داشبورد (آمار دوره/دانشجو/درآمد/تکالیف/پرسشها) + دورههای من + دانشجویان با پیشرفت + نمرهدهی تکالیف (با اعلان به دانشجو) + پاسخ پرسشها (با اعلان) + درآمد با سهم ۵۰٪

### 🔔 اعلانهای داخلی
- جدول Notification + صفحه `/dashboard/notifications` + شمارنده خواندهنشده در منو
- رویدادها: پاسخ تیکت، پرداخت موفق، صدور گواهی، نمره تمرین، پاسخ مدرس

### 📊 گزارشهای داشبورد ادمین
- فروش این ماه + تعداد سفارش + ثبتنام امروز + کاربران فعال ۷ روز + نرخ تکمیل دوره + میانگین پیشرفت (بهعلاوه نمودار ۷ روزه قبلی)

### 🗂 مدیریت صفحات ادمین
- `/admin/pages` با همه عملیات + سطل زباله + SEO + تاریخچه نسخهها

---

## ضمیمه: بررسی نهایی — موارد باقیمانده اضافه شد

بررسی جامع همه درخواستهای قبلی، ۱۰ مورد باقیمانده پیدا و پیادهسازی شد:

### 🆕 این جلسه اضافه شد
1. **قالب داینامیک صفحه دوره و مدرس** — ادمین یک بار قالب میسازد (ویجتهای `current_course` و `current_teacher`)، همه دورهها/مدرسها با آن نمایش داده میشوند و اطلاعات هرکدام داینامیک است (تست: صفحه دوره از قالب رندر شد ✅)
2. **🛡 صفحه استعلام گواهی عمومی** — `/verify-certificate` — هر کسی با کد رهگیری صحت گواهی را بررسی کند (تست: کد واقعی = معتبر ✅)
3. **🧠 آزمون استعدادیابی** — ۸ سوال → ۴ مسیر شغلی + دورههای پیشنهادی (تست: «توسعهدهنده وب» ✅)
4. **🗂 زیرمنو چندسطحی در منوساز** — ستون «شماره خط والد» + پیشنمایش سلسلهمراتبی (تست: زیرمنو با parent_id ✅)
5. **🔒 دسترسی تدریجی (Drip)** — فیلد «باز شدن بعد از N روز» برای هر جلسه + قفل 🔒 با شمارش معکوس روز در لیست جلسات
6. **📅 رزرو مشاوره با زمان** — زمان ترجیحی (صبح/ظهر/عصر/شب) + تاریخ در فرم مشاوره
7. **📱 ارسال گروهی پیامک** — از لیست کاربران: انتخاب نقش + متن → SMS به همه (حالت دمو)
8. **📊 خروجی اکسل دانشجویان هر دوره** — CSV با نام/موبایل/پیشرفت/گواهی + دکمه در پنل استاد
9. **🔥 اعلان تخفیف به علاقهمندان** — وقتی ادمین روی دوره تخفیف میگذارد، همه کسانی که دوره را ❤️ کردهاند اعلان میگیرند
10. **🎁 کد دعوت + وضعیت گیمیفیکیشن در پروفایل** — لینک معرف با دکمه کپی + امتیاز/استریک/نشان

### 🔧 رفع باگها
- helper های `bc_current_course/teacher` به jinja globals اضافه شد (قالب داینامیک 500 میداد)
- زیرمنو: `flush()` بعد از هر آیتم (id والد ست نمیشد) + `splitlines()` برای خطوط
- مدرس دمو (دکتر سارا محمدی) بازسازی شد — در تستهای قبلی حذف شده بود

### ✅ نتیجه نهایی
- **۵۲/۵۲ مسیر سالم**
- همه ۱۰ چت اخیر: پوشش کامل

---

## ضمیمه: بررسی نهایی دوم — ۱۳ مورد آخر اضافه شد

بررسی جامع دوباره، ۱۳ مورد باقیمانده پیدا کرد که همه پیاده‌سازی و تست شدند:

### 🆕 این جلسه
1. **💬 انجمن گفتگو** — تاپیک + پاسخ + پین + دسته‌بندی بر اساس دوره + امتیاز برای فعالیت + صفحه مدیریت ادمین (تست: ساخت تاپیک و پاسخ ✅)
2. **🎥 کلاس آنلاین (لایو)** — ساخت جلسه با لینک اسکای‌روم/گپ + تاریخ/مدت + رزرو توسط دانشجو + صفحه عمومی + مدیریت ادمین
3. **💳 پرداخت اقساطی داخلی** — انتخاب ۲/۳/۴ قسط در تسویه (بالای ۳۰۰ هزار) + قسط‌ها با تاریخ سررسید + پرداخت هر قسط + فعال‌سازی خودکار دوره پس از آخرین قسط (تست: ۳ قسط → paid ✅)
4. **🏅 مدیریت گواهی‌ها در ادمین** — لیست همه گواهی‌ها با کد رهگیری + لغو گواهی (با اعلان به دانشجو)
5. **📊 گزارش رفتار دانشجو** — نرخ تکمیل، میانگین پیشرفت، فهرست «در معرض افت» (زیر ۴۰٪)
6. **⏰ یادآور ادامه یادگیری** — بنر «ادامه بده!» برای دوره‌های راکد ۷+ روز در داشبورد
7. **✨ پیشنهاد شخصی‌سازی‌شده** — بر اساس دسته‌های علاقه‌مندی/دوره‌های قبلی (تست ✅)
8. **🎨 هدر/فوتر اختصاصی برای هر صفحه** — دکمه در مدیریت صفحات + فیلد custom_header/footer
9. **💬 زیرنویس ویدیو** — آپلود فایل srt/vtt برای هر جلسه + track در پلیر
10. **📺 انتخاب کیفیت ویدیو** — لینک HD برای هر جلسه + دکمه SD/HD در پلیر
11. **🎯 چک‌پوینت یادگیری** — بعد از هر ۵ جلسه کامل، بنر پیشنهاد کوییز
12. **🎁 اعلان تخفیف به علاقه‌مندان** — تخفیف جدید روی دوره → اعلان به همه ❤️کننده‌ها (در ویرایش دوره)
13. **📝 دیتای دمو** — کلاس آنلاین نمونه + تاپیک انجمن

### 🔧 رفع باگ
- پیشنهاد شخصی‌سازی‌شده: `Enrollment.category_id` → `c.course.category_id`

### ✅ نتیجه
- **۵۹/۵۹ مسیر سالم** — همه صفحات ۲۰۰

---

## ضمیمه: بررسی نهایی سوم — ۱۰ مورد آخر اضافه شد

بررسی کامل همه چکلیستهای ۱۰ چت اخیر، این موارد را هنوز ناقص نشان داد که همگی اضافه و تست شدند:

### 🆕 این جلسه
1. **📧 ایمیل SMTP واقعی** — تنظیمات (host/port/user/pass/from/TLS) در پنل + دکمه «ارسال ایمیل تست» + ارسال خودکار: خوشآمدگویی ثبتنام، تایید خرید، صدور گواهی (با کد رهگیری)، پاسخ تیکت — همه با قالب HTML زیبا (بدون SMTP = حالت دمو در لاگ)
2. **⭐ ذخیره سکشن به کتابخانه قالبها** — دکمه ⭐ روی هر ردیف در صفحهساز → ذخیره در `section_library.json` و استفاده مجدد
3. **🔒 قفل سکشن** — دکمه روی ردیفها؛ سکشن قفلشده قابل ویرایش نیست
4. **🗺 ناویگیتور صفحهساز** — نمای درختی زنده (ردیفها + ویجتها) کنار بوم با کلیک برای پرش/انتخاب
5. **📦 ذخیره صفحه به عنوان قالب** — دکمه در نوار ابزار → `page_library.json`
6. **📣 کمپین خبرنامه** — ارسال گروهی از پنل به همه ثبتنامشدهها از طریق ایمیل/پیامرسان/پیامک
7. **👥 چند مدرس برای دوره** — فیلد «مدرسهای کمکی» در فرم دوره + رابطه CourseTeacher + نمایش
8. **🗃 بانک سوال** — سوالات مشترک با دستهبندی + «افزودن به آزمون» با یک کلیک
9. **💳 نمایش اقساط در صفحه دوره** — «۲ قسط ماهانه از X تومان» در کارت خرید (هر دو حالت عادی و قالب داینامیک)
10. **📊 ویجت داینامیک داشبورد دانشجو** — ویجت `user_dashboard` در صفحهساز: دورههای من + کیف پول + تیکتها + فاکتورها

### ✅ نتیجه
- **۶۲/۶۲ مسیر سالم**
- تمام چکلیستهای ۱۰ چت اخیر: پوشش کامل — هیچ مورد قابل پیادهسازی باقی نمانده

---

## ضمیمه: بررسی نهایی چهارم — ۱۲ مورد آخر اضافه شد

بررسی کامل همه ۱۰ چت اخیر، این موارد باقیمانده را نشان داد که همگی اضافه و تست شدند:

### 🆕 این جلسه
1. **🎫 تیکت پیشرفته** — دستهبندی + اولویت (فوری/زیاد/معمولی/کم) + **ارجاع به پشتیبان** + **فایل پیوست** + **تاریخچه مکالمه کامل** (جدول ticket_replies) + فیلترها
2. **📊 گزارش عملکرد پشتیبانی** — میانگین زمان پاسخ + عملکرد هر پشتیبان
3. **🔍 جستجو/فیلتر کاربران** — نام/ایمیل/موبایل/کد ملی + فیلتر نقش
4. **💰 مدیریت کیف پول در ادمین** — افزایش/کاهش موجودی هر کاربر با یادداشت (تست: +100k/−50k ✅)
5. **💳 مدیریت اقساط در ادمین** — همه قسطها با سررسید و وضعیت + فیلتر
6. **👥 ویرایش دسترسی نقشها** — چکباکسهای Permission-Based برای هر نقش (ذخیره در تنظیمات)
7. **🎨 لوگو و برند** — آپلود لوگو (نمایش در هدر) + رنگهای سفارشی برند (CSS زنده) + متن/امضای گواهینامه
8. **📈 گزارش درآمد هر دوره** — فروش کل + تعداد دانشجو
9. **👁 صفحات پربازدید** — ۵۰ صفحه برتر
10. **😴 کاربران غیرفعال** — بدون فعالیت ۳۰ روز
11. **🔍 سلامت سئو** — صفحات بدون عنوان/توضیحات متا
12. **🔒 حفاظت فایلهای درس** — دانلود فقط برای ثبتنامیها/ادمین + مسدودسازی /static/uploads در robots
13. **🔔 اعلانهای رویدادی** — پرداخت ناموفق + جلسه جدید (به دانشجویان دوره) + کلاس نزدیک + خرید ناقص (یادآور روزانه)

### 🔧 رفع باگ
- رابطه Ticket با دو FK به User → foreign_keys مشخص شد

### ✅ نتیجه
- **۷۱/۷۱ مسیر سالم**
- تمام چکلیستهای ۱۰ چت اخیر: پوشش کامل

---

## ضمیمه: بررسی نهایی پنجم — ۵ مورد آخر اضافه شد

### 🆕 این جلسه
1. **💬 پیام خصوصی (DM)** — صندوق پیام با لیست گفتگوها + نشان «جدید» + مکالمه دوطرفه + API (تست: ارسال و دریافت ✅)
2. **📅 برنامه کلاسی من** — کلاسهای پیشروی دورههای کاربر + رزروهای قبلی (ویجت «برنامه کلاسی» چکلیست)
3. **🗳 نظرسنجی انجمن** — ساخت نظرسنجی در تاپیک + رای + نمایش درصد (تست: رای ثبت و نمایش داده شد ✅)
4. **💬 رضایت سنجی پس از دوره** — امتیاز ۱-۵ با ستاره + پیشنهاد + نظر + دکمه در «دورههای من»
5. **🗄 بازیابی بکاپ از پنل** — لیست بکاپها + دانلود + بازیابی با بکاپ خودکار قبل از بازگردانی + فیلتر زمان شمسی

### 🔧 رفع باگ
- بازگردانی بکاپ جدولهای جدید را حذف کرده بود → دیتابیس بازسازی شد + بکاپ تازه با همه ۵۳ جدول گرفته شد
- رای نظرسنجی: ترتیب پردازش POST اصلاح شد
- فیلتر `timestamp_to_jdate` برای زمان بکاپها

### ✅ نتیجه
- **۷۵/۷۵ مسیر سالم** — همه صفحات ۲۰۰

---

## ضمیمه: بررسی نهایی ششم — ۷ مورد + ۳ باگ رفع شد

بررسی کامل دوباره همه چکلیستهای ۱۰ چت اخیر + تست عملی هر مسیر:

### 🆕 این جلسه اضافه شد
1. **⏱ آزمون زماندار (تایمر شمارش معکوس)** — فیلد `time_limit` حالا واقعاً اجرا میشود: نوار تایمر + ارسال خودکار پاسخها در پایان زمان + هشدار قبل از بستن صفحه (نیاز «آزمون زماندار» سایت کنکوری)
2. **🔔 اعلان گروهی از پنل ادمین** — صفحه `/admin/notifications`: ارسال به همه / نقش خاص / دانشجویان یک دوره + لیست اعلانهای اخیر + لاگ (مورد «ارسال پیام گروهی» چکلیست)
3. **🎯 داشبورد افیلیت کامل** — صفحه دعوت دوستان حالا: سطح همکاری (نقرهای/برنزی/طلایی/الماس) + نوار پیشرفت + حجم خرید دعوتشدهها + جدول عملکرد هر دوست + تاریخچه پاداشها
4. **🔥 پیشنهاد لحظهای خرید (Upsell)** — بعد از افزودن به سبد، مودال دوره مرتبط با تخفیف نمایش داده میشود (مورد «پیشنهاد لحظهای خرید» چکلیست)
5. **👥 فیلد «مناسب برای چه افرادی»** — ستون `audience` در دورهها + نمایش در تب «درباره دوره» + فیلد فرم ادمین
6. **🔔 تنظیمات اعلان شخصی کاربر** — چکباکس «ایمیل/پیامک اطلاعرسانی» در پروفایل + احترام خودکار در ارسال ایمیلها، پیامک خرید و ارسال گروهی (OTP و امنیتی همیشه ارسال میشوند)
7. **📅 مدت دسترسی به دوره (Access Days)** — فیلد «مدت دسترسی (روز)» حالا واقعاً کار میکند: نمایش در صفحه دوره + اتمام دسترسی با پیام تمدید

### 🐛 باگهای رفعشده
1. **500 در `/learn/2`** — کد از `course.access_days` و `intro_video` استفاده میکرد ولی ستونها در مدل نبودند (فقط در DB) → به مدل اضافه شد + فرم ادمین مقادیر را ذخیره میکند
2. **منوی منشی خراب** — مسیر `admin.consultations` وجود نداشت ولی در منو بود → صفحه «درخواستهای مشاوره (لیدها)» ساخته شد (خواندن/حذف/جدید)
3. **405 در ساخت منو** — `/admin/menus/new` فقط POST بود → GET حالا به فهرست منوها ریدایرکت میشود

### ✅ نتیجه
- **تست مرورگری (Playwright)**: مودال آپسل ✅ · تایمر آزمون ✅ · صفحات جدید ادمین ✅ · learn/2 ✅ · پروفایل ✅ · افیلیت ✅
- **۱۲۳ مسیر تستشده** — همه ۲۰۰؛ موارد 404 باقیمانده، مسیرهای معادل (دسته = `/courses?cat=`، جستجو = `/courses?q=`، و...) بودند
- **بکاپ تازه** گرفته شد


---

## ضمیمه: بررسی نهایی هفتم — شبیه‌ساز آزمون + داستان‌های موفقیت + ۴ گزارش جدید

### 🆕 این جلسه اضافه شد
1. **🎯 شبیه‌ساز آزمون (کنکور)** — `/exam/practice`: انتخاب دسته و تعداد سوال → سوالات تصادفی از بانک سوال با **تایمر ۴۵ ثانیه‌ای** و ارسال خودکار → نمره‌دهی + **مرور پاسخ‌ها با توضیح** + تاریخچه با میانگین/بهترین + **گزارش ادمین** (شرکت‌کننده‌ها، میانگین، عملکرد هر دسته)
2. **🌟 داستان‌های موفقیت (نتایج واقعی دانشجویان)** — جدول `success_stories` + مدیریت کامل در ادمین (`/admin/success-stories`) + **ویجت داینامیک صفحه‌ساز** (در هر ۵ طرح صفحه اصلی) + صفحه اختصاصی `/success-stories` + ۴ داستان دمو
3. **📊 ۴ گزارش جدید ادمین** — مجموع گزارش‌ها به ۱۲ نوع رسید:
   - `🎟 گزارش تخفیف‌ها` — مصرف هر کوپن + تخفیف داده‌شده + فروش ایجادشده
   - `⭐ گزارش رضایت‌سنجی` — میانگین امتیاز هر دوره + درصد پیشنهاد به دیگران
   - `💰 گزارش درآمد مدرس‌ها` — فروش هر مدرس + سهم ۵۰٪ + تسویه‌شده/در انتظار
   - `🎯 گزارش آزمون‌ها` — شرکت‌کننده‌ها + میانگین درصد + عملکرد هر دسته
4. **🧠 بانک سوال پر شد** — ۱۵ سوال استاندارد در ۵ دسته (برنامه‌نویسی، طراحی وب، ریاضی و...) + ۵ بازخورد دوره دمو
5. **🎯 ویجت «شبیه‌ساز آزمون (CTA)»** در صفحه‌ساز — بنر جذاب با دکمه شروع در هر ۵ طرح صفحه اصلی
6. **لینک «شبیه‌ساز آزمون» در داشبورد دانشجو** + منوی ادمین (داستان موفقیت + ۴ گزارش)

### 🐛 باگ رفع‌شده
- ثبت آزمون 500 می‌داد (لیست/دیکشنری به‌جای JSON در ستون‌ها) → `json.dumps` اضافه شد
- `round()` در قالب گزارش آزمون → فیلتر `|round|int` جایگزین شد

### ✅ نتیجه
- **۱۲۳/۱۲۳ مسیر سالم** (تست خودکار `scripts/health_check.py`)
- **تست مرورگری**: شبیه‌ساز آزمون کامل (شروع ← اجرا ← ثبت ← مرور) ✅ · ویجت‌های صفحه اصلی ✅ · گزارش‌ها ✅ · داستان‌ها ✅


---

## ضمیمه: بررسی نهایی هشتم — اسکن سراسری همه مسیرها + رفع ۳ باگ

### 🔍 اسکن جامع (نه تک‌به‌تک!)
اسکریپت `scripts/full_route_test.py` نوشته شد: **همه مسیرهای اپ از url_map** با **مقادیر واقعی دیتابیس** (اسلاگ دوره‌ها، آیدی‌ها، کد سفارش‌ها و...) در ۳ حالت (مهمان/دانشجو/ادمین با ۲FA) + مسیرهای POST + مسیرهای Flask-Admin + **بررسی ۲۵ ارجاع استاتیک**:

### 🐛 ۳ باگ واقعی پیدا و رفع شد
1. **500 در «پاسخ‌های فرم»** (`/admin/forms/<id>/entries`) — فیلتر `from_json` وجود نداشت → به فیلترهای سراسری Jinja اضافه شد
2. **GET روی مسیرهای POST-only → 405** — هندلر سراسری `errorhandler(405)` اضافه شد: ۴۰+ مسیر POST-only (تست درگاه، پیام‌رسان، نمره‌دهی، زمان‌بندی صفحه، خروج از همه دستگاه‌ها و...) حالا به صفحه مناسب ریدایرکت می‌شوند (تست مرورگری ✅)
3. **`/builder/new` با GET → 404** (تداخل با `/builder/<slug>`) → ریدایرکت به صفحه‌ساز

### 🆕 موارد تکمیلی
- **ویرایش باندل** — مسیر `/admin/bundles/<id>/edit` وجود نداشت (فقط ساخت/حذف) → فرم ویرایش کامل با انتخاب دوره‌ها و وضعیت فعال + دکمه ✏️ در لیست

### ✅ نتیجه
- **۱۹۵ تست GET + ۱۲ تست POST + ۱۳ مسیر Flask-Admin = صفر مشکل**
- ۲۵ ارجاع استاتیک: همه موجود ✅
- اسکریپت سلامت قبلی: ۱۲۳/۱۲۳ ✅
- بکاپ تازه: `academy-20260806-0823.db`
