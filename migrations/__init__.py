# -*- coding: utf-8 -*-
"""مایگریشن‌های داده‌ای نسخه‌بندی‌شده (در مسیر بروزرسانی خودکار اجرا می‌شوند).

قوانین:
  * نام فایل حتماً ``NNNN_نام.py`` باشد (NNNN چهار رقم؛ ترتیب اجرا = ترتیب نام).
  * تابع ``up(conn)`` را تعریف کنید؛ conn اتصال داخل تراکنش است (SQLAlchemy).
  * هر نسخه فقط یک بار اجرا می‌شود (ثبت در جدول schema_migrations).
  * مایگریشن‌ها باید idempotent و بی‌خطر برای دادهٔ موجود باشند.
  * شکست یک مایگریشن کل بروزرسانی را متوقف می‌کند تا دادهٔ سایت سالم بماند.
"""
