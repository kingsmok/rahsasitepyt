flask>=3.0
flask-sqlalchemy==3.1.1
SQLAlchemy==2.0.30
greenlet==3.0.3
requests>=2.31
# Pillow — نسخه امن (۹.۵ و پایین‌تر چند CVE دارند). Pillow 11+ از پایتون 3.9 پشتیبانی می‌کند
Pillow>=11.0.0
reportlab>=4.0
# cryptography — نسخه امن (41.0.7 و پایین‌تر CVE دارند). 42+ از پایتون 3.7 پشتیبانی می‌کند
cryptography>=42.0.0
flask-admin>=1.6
gunicorn>=22.0
python-dotenv>=1.0
pytest>=8.0
redis>=5.0
PyMySQL>=1.1
