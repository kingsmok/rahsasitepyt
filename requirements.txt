# افزونه‌های باینری روی هاست اشتراکی نباید از سورس کامپایل شوند.
# نسخه‌های زیر برای CPython 3.9 تا 3.14 و Linux manylinux2014 wheel آماده دارند.
--only-binary=greenlet,Pillow

flask>=3.0
flask-sqlalchemy==3.1.1
SQLAlchemy==2.0.30
# 3.0.3 روی Python 3.13+ wheel ندارد و کامپایل آن در cPanel شکست می‌خورد.
greenlet==3.2.5
requests>=2.31
defusedxml>=0.7.1
# Pillow 12.3 دیگر wheel سازگار با glibc 2.17 هاست‌های قدیمی ندارد.
# 11.3 آخرین نسخهٔ سازگار با Python 3.9 و 12.2 آخرین نسخه با manylinux2014 wheel است.
Pillow==11.3.0; python_version < "3.10"
Pillow==12.2.0; python_version >= "3.10"
reportlab>=4.0
# شکل‌دهی و جهت صحیح متن فارسی در PDF فاکتور/گواهی
arabic-reshaper>=3.0.0
python-bidi>=0.6.0
# cryptography — نسخه امن (41.0.7 و پایین‌تر CVE دارند). 42+ از پایتون 3.7 پشتیبانی می‌کند
cryptography>=42.0.0
flask-admin>=1.6
gunicorn>=22.0
python-dotenv>=1.0
pytest>=8.0
redis>=5.0
PyMySQL>=1.1
