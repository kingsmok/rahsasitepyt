# ────────────────────────────────────────────────────────────────────
# سیاست نصب: هرگز از سورس کامپایل نکن.
#
# خطای «Failed building wheel for greenlet / Pillow» یعنی pip برای پایتونِ
# این هاست wheel آماده پیدا نکرده و رفته سراغ کامپایل از سورس — که روی
# هاست اشتراکی (بدون gcc / python3-dev و با محدودیت رم) شکست می‌خورد.
#
# دو خط زیر جلوی این اتفاق را می‌گیرند:
#   --prefer-binary  : همیشه نسخه‌ای که wheel دارد ترجیح داده شود
#   --only-binary    : برای پکیج‌های C-extension اصلاً سراغ سورس نرو
# (اگر روی معماری نادری هستید که wheel ندارد و gcc دارید، خط --only-binary
#  را موقتاً کامنت کنید.)
# ────────────────────────────────────────────────────────────────────
--prefer-binary
--only-binary greenlet,Pillow,cryptography,SQLAlchemy

flask>=3.0
flask-sqlalchemy==3.1.1
# SQLAlchemy روی خط ۲.۰ می‌ماند (۲.۱ تغییرات ناسازگار دارد)
SQLAlchemy>=2.0.30,<2.1
# ⚠️ greenlet عمداً اینجا پین نشده است.
# قبلاً greenlet==3.0.3 پین بود؛ آن نسخه فقط تا پایتون ۳.۱۲ wheel دارد،
# پس روی هاست‌های پایتون ۳.۱۳/۳.۱۴ نصب می‌رفت سراغ کامپایل و می‌شکست.
# greenlet فقط برای حالت asyncio لازم است (این پروژه از آن استفاده نمی‌کند) و
# خودِ SQLAlchemy در صورت نیاز نسخهٔ درست و دارای wheel را نصب می‌کند.
requests>=2.31
defusedxml>=0.7.1
# Pillow — نسخه امن (۹.۵ و پایین‌تر چند CVE دارند). سقف نمی‌گذاریم تا روی
# پایتون جدید هم wheel آماده بگیرد؛ روی پایتون ۳.۹ خودکار ۱۱.۳ نصب می‌شود.
Pillow>=11.0.0
reportlab>=4.0
# شکل‌دهی و جهت صحیح متن فارسی در PDF فاکتور/گواهی
arabic-reshaper>=3.0.0
python-bidi>=0.6.0
# cryptography — نسخه امن (41.0.7 و پایین‌تر CVE دارند). 42+ از پایتون 3.7 پشتیبانی می‌کند
cryptography>=42.0.0
flask-admin>=1.6
gunicorn>=22.0
python-dotenv>=1.0
pytest>=8.0
redis>=5.0
PyMySQL>=1.1
