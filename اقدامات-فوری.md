# ✅ چه کار کنم؟ — چک‌لیست عملی (به ترتیب)

> خلاصهٔ وضعیت: سایت شما به‌خاطر زیردامنهٔ فیشینگ
> `xfinity.billing-account.update.diier.ir` علامت خورده است.
> آن زیردامنه **الآن دیگر فعال نیست** (DNS آن حذف شده) — پس بخشی از کار
> قبلاً انجام شده. کار باقی‌مانده: بستن راه نفوذ + درخواست بازبینی از گوگل.

⏱ زمان کل: حدود ۴۰ دقیقه کار + ۱ تا ۳ روز انتظار برای گوگل

---

## 🔴 گام ۱ — پنل DNS را تمیز کنید (۱۰ دقیقه)

وارد پنل مدیریت DNS دامنه شوید (ایرنیک یا هرجا که DNS را مدیریت می‌کنید).

**باید فقط این رکوردها باشند:**

| نوع | نام | مقدار |
|-----|-----|-------|
| A | `@` (یا diier.ir) | `87.236.209.235` |
| A یا CNAME | `www` | `87.236.209.235` |
| MX / TXT | (اگر ایمیل دارید) | مقادیر خودتان |

**هر رکورد دیگری که خودتان نساخته‌اید را حذف کنید** — مخصوصاً:
- هر رکورد `*` (wildcard) — این خطرناک‌ترین است
- هر زیردامنه با کلمات: `xfinity`, `billing`, `update`, `verify`, `secure`, `login`

> 💡 اگر رکورد `*` (ستاره) دارید، حتماً حذفش کنید. با آن، هر زیردامنه‌ای
> که مهاجم بسازد خودکار به سرور شما وصل می‌شود.

---

## 🔴 گام ۲ — همهٔ رمزها را عوض کنید (۱۰ دقیقه)

کسی توانسته زیردامنه بسازد، پس یکی از این‌ها لو رفته:

- [ ] پنل ثبت دامنه (ایرنیک / رجیسترار)
- [ ] پنل DNS
- [ ] پنل هاست / سی‌پنل
- [ ] رمز SSH سرور
- [ ] پنل مدیریت خود سایت (`/admin`)

**تایید دومرحله‌ای** را هرجا که ممکن است روشن کنید.

در پنل سایت هم: **کاربران → خروج از همهٔ دستگاه‌ها**

---

## 🟠 گام ۳ — گواهی wildcard را باطل کنید (۵ دقیقه)

الآن گواهی `*.diier.ir` دارید. یعنی **هر زیردامنه‌ای** خودکار SSL معتبر
می‌گیرد — این همان چیزی است که کار مهاجم را راحت کرد.

```bash
sudo certbot revoke --cert-name diier.ir
sudo certbot --nginx -d diier.ir -d www.diier.ir
sudo systemctl reload nginx
```

بعدش تست کنید که سایت بالاست: `https://diier.ir`

> اگر روی هاست اشتراکی (سی‌پنل) هستید و به certbot دسترسی ندارید،
> از پشتیبانی هاست بخواهید گواهی wildcard را حذف و گواهی ساده صادر کنند.

---

## 🟠 گام ۴ — سرور را بگردید (۱۰ دقیقه)

```bash
# ۱) پوشه‌های وب — چیزی غیر از آکادمی هست؟
ls -la /var/www/ /home/*/public_html/ 2>/dev/null

# ۲) فایل PHP — پروژهٔ ما پایتون است، نباید هیچ php داشته باشد!
find / -name "*.php" -mtime -400 -not -path "*/node_modules/*" 2>/dev/null | head -30

# ۳) هر چیزی به نام xfinity یا billing
find / \( -iname "*xfinity*" -o -iname "*billing*" \) 2>/dev/null | head -20

# ۴) کانفیگ‌های اضافی وب‌سرور
grep -rn "server_name" /etc/nginx/sites-enabled/ 2>/dev/null
grep -rn "ServerName\|ServerAlias" /etc/apache2/sites-enabled/ 2>/dev/null
```

اگر چیز مشکوکی پیدا شد → حذف کنید و به من بگویید.

---

## 🟢 گام ۵ — بروزرسانی کد آکادمی (۵ دقیقه)

```bash
cd /path/to/site
git pull

# پاک‌سازی محتوای نمایشی/جعلی از دیتابیس زنده
python scripts/clean_demo_content.py            # اول گزارش
python scripts/clean_demo_content.py --apply    # بعد اعمال

# عیب‌یابی نهایی (شامل بررسی زیردامنه‌های مشکوک)
python scripts/safe_browsing_check.py https://diier.ir

# ری‌استارت
touch passenger_wsgi.py          # هاست اشتراکی
# یا:  sudo systemctl restart academy
```

سپس در **پنل مدیریت → تنظیمات**، اطلاعات واقعی خود را وارد کنید:
شماره تماس (`۰۹۹۰۷۲۳۱۱۷۳`؟)، ایمیل، آدرس، آیدی شبکه‌های اجتماعی.

⚠️ صفحات **«قوانین و مقررات»** و **«حریم خصوصی»** را هم با متن واقعی پر کنید.
سایتی که پرداخت آنلاین دارد و این صفحات را ندارد، دوباره علامت می‌خورد.

---

## 🟢 گام ۶ — درخواست بازبینی از گوگل (۱۰ دقیقه) ⭐ مهم‌ترین

**بدون این گام، هشدار برطرف نمی‌شود** — حتی اگر همه‌چیز تمیز باشد.

### الف) دامنه را به Search Console اضافه کنید

<https://search.google.com/search-console>

- «Add property» → **URL prefix** → `https://diier.ir`
- تأیید مالکیت: راحت‌ترین راه، آپلود فایل HTML در ریشهٔ سایت
  (فایل را در پوشهٔ `static/` بگذارید یا از تأیید با رکورد DNS استفاده کنید)

### ب) گزارش را ببینید

منوی راست → **Security & Manual Actions** → **Security issues**

اینجا گوگل **دقیقاً می‌گوید کدام URL** مشکل دارد. اگر URL دیگری غیر از آن
زیردامنه نشان داد، حتماً به من بگویید.

### ج) روی **REQUEST REVIEW** بزنید و این متن را کپی کنید:

```
Hello,

My domain diier.ir was flagged for social engineering / deceptive content.

Root cause identified:
A phishing subdomain "xfinity.billing-account.update.diier.ir" existed on my
domain (an SSL certificate for it appears in Certificate Transparency logs,
issued 2025-08-20). This subdomain impersonated an Xfinity/Comcast billing
page. It was created without my authorization.

Actions taken:
1. Removed all unauthorized DNS records; only diier.ir and www remain.
2. Revoked the wildcard certificate (*.diier.ir) and issued a certificate
   limited to diier.ir and www.diier.ir, so no arbitrary subdomain can obtain
   a valid certificate.
3. Rotated all credentials: domain registrar, DNS panel, hosting control
   panel, SSH, and site administrator accounts. Enabled 2FA where available.
4. Audited the server: no PHP files, web shells, or unknown virtual hosts
   remain. The phishing hostname no longer resolves.
5. Hardened the application: blocked upload of executable/HTML/SVG content,
   added strict Content-Security-Policy, sandboxed all uploaded files, and
   removed any misleading content.

The domain now hosts only a legitimate Persian-language e-learning website.

Please re-review. Thank you.
```

### د) منتظر بمانید

معمولاً **۱ تا ۳ روز کاری**. وضعیت را اینجا چک کنید:
<https://transparencyreport.google.com/safe-browsing/search?url=diier.ir>

---

## ❓ سوالی که باید جوابش را بدانید

**آیا دامنهٔ `diier.ir` را دست‌دوم خریده‌اید؟**

اولین گواهی SSL این دامنه مربوط به **دسامبر ۲۰۲۳** است و زیردامنهٔ فیشینگ
**آگوست ۲۰۲۵** ساخته شده.

- اگر **بله، دست‌دوم خریده‌اید** → کار مالک قبلی بوده. فقط گام‌های بالا کافی
  است و در Request Review این را هم بنویسید:
  *"I acquired this domain after the abuse occurred."*

- اگر **نه، از اول مال شما بوده** → یعنی واقعاً به حساب شما نفوذ شده و
  گام‌های ۱ تا ۴ (مخصوصاً تعویض رمزها) **حیاتی** هستند.

---

## 📌 بعد از رفع هشدار

```bash
# قبل از هر انتشار، این‌ها را اجرا کنید
python -m pytest tests/test_safe_browsing.py -q
python scripts/safe_browsing_check.py https://diier.ir
```

- دامنه را در Search Console نگه دارید تا هشدار بعدی را زود ببینید
- هرگز رکورد DNS از نوع `*` (wildcard) نسازید
- گواهی wildcard نگیرید مگر واقعاً لازم باشد
